# Event planning details

Follow the parent skill's ownership and approval boundaries.

## Scope

Search existing event records before creating a slug or folder. Recover the event type, date/range, location, headcount, format, owner and known budget from Brian's request and existing sources. Ask only for missing constraints that prevent useful work; budget can remain unknown. Reuse established event slugs rather than a historical hardcoded seed list.

Record confirmed scope, open questions, source links and tentative choices in the owning event's task folder. If an existing KMG-only local state file is used by the workflow, preserve its fields: slug/name, createdAt, type, targetDate/time range, city/neighborhood, headcount, vibe, budgetCeiling, venuesShortlist, vendorsByCategory, openInquiries, decidedVenue/decidedVendors and ros. Do not populate unknown fields with invented facts or copy S&W private work into that state.

## Run of show

Use the event's actual date, venue, format, headcount, speakers and operational constraints. Draft an appropriate timeline from setup through check-in, programming, networking and wrap; choose block lengths that fit the event rather than a fixed template. Each block needs time, activity, owner and relevant dependencies. Mark undecided owners/times as tentative.

Save the run of show with the owning event's files and link it from the current event record. If the existing KMG workflow already owns `data/events/<slug>/ros.md`, preserve that path. Present the draft without claiming venues, speakers or dates are committed.
