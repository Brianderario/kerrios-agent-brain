---
name: sw-linkedin-connect-routine
description: Daily weekday routine that emails Brian 20 LinkedIn people to connect with for Standard & Works (hard-tech execs + investors) with a soft note
metadata: 
  node_type: memory
  type: project
  originSessionId: dcb944d5-6802-4073-a71e-27887ebae9b2
---

**`kerri-sw-linkedin-connect`** — built 2026-06-20. Weekday ~7:45am ET (`45 7 * * 1-5`) scheduled task that emails Brian (kerri@hardwarefyi.com → brian@kerrihq.com, internal digest, no approval gate) **20 net-new people to connect with on LinkedIn to grow the Standard & Works readership**: 15 hard-tech company execs (CEO/COO/CFO/founder/president) + 5 investors, each with name, title, company, LinkedIn URL, and one short paste-ready soft connect note. Brian does the actual connecting; the routine never connects or emails anyone but Brian.

- **Seed universe:** the Relay "Builders" 533-company directory at `~/.claude/skills/hard-tech-roundup/references/relay-builders-directory.md` (the same "anybody building hardware in America" list the S&W newsletter writer uses as its master company-tracking directory) + a curated investor-firm seed (live source = Relay VC tab).
- **Sourcing:** Apollo. Search by company domain + senior titles → enrich each person by Apollo `id` (`apollo_people_match`) to reveal full name + `linkedin_url` (search alone masks last names and omits LinkedIn URL). ~50 credits/run, pre-approved.
- **Dedupe + cursors:** `data/sw-linkedin-connect/state.json` (suggested Apollo ids + normalized LinkedIn URLs, companyCursor, investorCursor). Ledger per run in `data/sw-linkedin-connect/runs/`; one line to `brain/log.md`. HTML artifact in `output/sw-linkedin-connect/`.
- **Note style (Brian's call):** very brief, generic ("thought Standard & Works might be valuable for you"), first-name only, no personalization, 3 rotating variants, under 300 chars, no em dashes. Don't claim Brian founded/owns S&W ("helps put together"/"works on").
- **Subscribe CTA (added 2026-06-22, Brian reversed his earlier no-link call):** each connect note now carries a quick subscribe CTA. Invite notes use the bare readable domain `standardandworks.com` (LinkedIn does NOT hyperlink URLs inside connection-request notes). The full clickable, UTM-tagged link `https://www.standardandworks.com/?utm_source=linkedin&utm_medium=social&utm_campaign=brian-li` lives in a new optional post-accept follow-up message, shown per-card in the digest (LinkedIn DOES hyperlink in messages). Both wired into the canonical SKILL.md STEP 2 + STEP 3.
- **Canonical prompt:** `agent-prompts/kerri-sw-linkedin-connect/SKILL.md`. Registered in `routines.manifest.json` (monitored:false) + `CLAUDE-ROUTINES.md` Loop 3. NOT in the HWFYI sponsor pipeline/CRM — this is S&W public audience growth, distinct from the older [[useful_skills_newsletter]]-era LinkedIn *content* motion (posting, candidate `2026-06-09-linkedin-content-motion`).
- First live batch sent 2026-06-20 (Sat); next scheduled run Mon 2026-06-22. Roster barely touched (cursor 5/533).
- **Traversal is SHUFFLED, not Relay's native order (Brian rule 2026-06-23).** Relay's file order is sector-clustered (a long space/aerospace/defense block at the front), so the sequential walk made early daily batches almost pure A&D (the Jun 23 batch, cursor 26→56, was ~16/20 space/aero/defense). Fix: the routine now walks `state.rosterOrder` — a fixed shuffled permutation of the 533 companies (already-walked 56 pinned at front, remaining 477 shuffled deterministically: mulberry32, seed 20260623, Fisher-Yates; see `state.rosterOrderMeta` + builder `data/sw-linkedin-connect/build-shuffle.mjs`). So each batch is now a cross-section of all 21 sectors. SKILL.md STEP 0/1a/appendix updated to walk `rosterOrder` + reconcile re-pulls + self-heal if state lost. **Caveat:** shuffle de-clusters but can't make A&D a minority — the universe is genuinely majority A&D-tagged (space 139 + defense 111 + aero 74 of 533), so A&D stays the largest single bloc per batch, just not the whole list. A hard per-batch A&D cap would need per-company sector tags (a live Relay re-pull) — offered to Brian, not yet built.
