---
name: brand-data-wall-build
description: "Per-brand data wall build in kerrihq-rails — stacked PR chain, worktree location, decisions, and rollout state"
metadata: 
  node_type: memory
  type: project
  originSessionId: 4fdc3dbc-a4e4-4a56-9900-0e32cf9677d5
---

Per-brand data wall for Savant Console + agents (approved by Brian 2026-07-01). Benji sees ONLY Hardware FYI everywhere (web, API, Slack agent); Ari = full master match with Brian (via `memberships.master_access` flag, NOT owner role); Zach = S&W (defense-in-depth; he's already an event_guest). Untagged data: backfill sweep then hidden (KMG catch-all property = fail closed). Benji sees HFYI revenue only, no org goal. Benji's console view = settings-based (UserViewPreference, agent-editable, approval-gated), never code.

**Status: LIVE IN PRODUCTION 2026-07-01.** `brand_enforcement` is ON. Merged via #255 then rollup #276 (the stacked chain #256–#271 broke when #272–#274 landed mid-merge; #276 carried phases 2–8 in one conflict-checked merge, suite 2608 green + eval ALL GREEN) plus #279 (backfill rule: restricted assignee's cards get their brand — found via the prod report, 43 of Benji's cards saved from KMG-default). Prod rollout executed via Render one-off jobs (jobs API with `startCommand`): db:seed personas → APPLY=1 brand:backfill (758 tags + 588 company taggings, report reviewed first) → brand:enforce. Verified live: Benji 122/125 deals (HFYI-only, Kinetic URL 404s, locked pill, revenue page HFYI grant), Ari=Brian parity, all 105 of Benji's board cards intact, agent prompt scope block Benji-only, KERRIHQ_AGENT_API_KEY org-wide unchanged. Rollback anytime: `rake brand:unenforce` (Render job). Retag queue: masters retag KMG-defaulted records via deal/event forms + revenue page. Worktree `~/Projects/kerrihq-rails-brand-wall` kept for follow-ups. Plan file: `~/.claude/plans/one-of-my-biggest-spicy-moonbeam.md`.

**Key mechanics:** `BrandScope` service is the single enforcement point (`.for/.apply/.wall/.wall_companies/.wall_people/.record_visible?`), gated on `PlatformSetting.brand_enforcement?`. Brand = `Property` (KMG/kmg slug is the catch-all); Domain stays the functional brain axis. PermissionGrant gained `property_id` (4th dimension). Companies are multi-tagged via `company_property_taggings`. `rake brand:backfill` (report CSV) / `APPLY=1` applies; prod run must happen BEFORE the flip, after Brian reviews the CSV.

**Env gotchas:** worktree needs `PATH=/opt/homebrew/opt/ruby@3.4/bin:/opt/homebrew/opt/postgresql@17/bin` prefixed (system ruby is 4.0 and breaks bundler); browser system specs need built assets copied from the main checkout (`app/assets/builds/`, homebrew node@22 is broken — missing simdjson dylib); eval harness needs `source ~/.kerri-chief/secrets/ollama.env` or every case errors "Ollama API key not configured". CRITICAL: worktrees share Postgres DBs with the main checkout by default — another agent's rspec boot reloads `kerrihq_rails_test` from THEIR schema.rb and wipes your migrations. Use the dedicated DBs via `DATABASE_URL=postgres://localhost/kerrihq_rails_brand_wall_development` (dev+seed) and `..._brand_wall_test` (rspec) for ALL rails/rspec commands in this worktree.

**How to apply:** finish remaining PRs in order; nothing enforces until PR8 flips `brand_enforcement`. KERRIHQ_AGENT_API_KEY must stay unbound/nil-property (org-wide) — regression spec guards this in PR6.
