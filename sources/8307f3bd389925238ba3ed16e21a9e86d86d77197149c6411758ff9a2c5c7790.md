---
name: mercury-access
description: Kerri has read+write Mercury bank API access for Hardware FYI; MCP server at ~/.kerri-chief/mercury-mcp/, credential location + handling rules
metadata: 
  node_type: memory
  type: reference
  originSessionId: 852fb982-3563-4b2f-9b59-8cbfc9e7f63b
---

Kerri has **read+write access to Hardware FYI's Mercury bank accounts via the Mercury REST API** (base `https://api.mercury.com/api/v1`, `Authorization: Bearer <token>`). A custom MCP server (`mercury-mcp`) was built 2026-06-22 and registered as the `mercury` MCP in Claude Code.

- **MCP server:** `~/.kerri-chief/mercury-mcp/` — TypeScript, built to `dist/index.js`, run via `run-mcp.sh`. Tools: `list_accounts`, `get_transactions`, `list_customers`, `create_customer`, `get_customer`, `list_invoices`, `get_invoice`, `create_invoice`, `update_invoice`, `cancel_invoice`.
- **Token location:** `~/.kerri-chief/secrets/mercury.env` (mode 600, NOT a git tree). `MERCURY_API_TOKEN` (read+write wma token), `MERCURY_API_BASE`.
- **NEVER** put token values in the KMG brain, GitHub, or any memory file.
- **Write token appeared in chat transcript 2026-06-22 — should be regenerated in Mercury dashboard when convenient** (revoke current wma token, create new one, update `.env` + `mercury-mcp/.env`). IP whitelist needed: `72.239.106.61` + `2603:9000:b00:25:1898:3fb:7947:dc9d`.
- **No money movement without Brian + Ari sign-off.** Invoice creation/sending is permitted autonomously; transfers are not.
- Legacy read-only tokens commented out in `mercury.env` — can be revoked.
- Accounts: Checking ••4504 ($24k), Savings ••4721 ($24k), Checking ••3822 "Vendor Payments" ($7.5k).
