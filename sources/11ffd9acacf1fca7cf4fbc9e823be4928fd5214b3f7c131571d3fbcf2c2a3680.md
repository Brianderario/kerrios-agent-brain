---
name: duplicate-inbox-sweep-systems
description: "Two parallel inbox-sweep systems both draft HWFYI sponsor replies, causing duplicate Console cards; the legacy codex worker is the second source."
metadata: 
  node_type: memory
  type: project
  originSessionId: 34aaf06c-3c28-492e-845a-f8a40a183306
---

As of 2026-06-30, **two** inbox-sweep systems run in parallel and both draft HWFYI sponsor replies, so Brian sees the same email twice in the Savant approval queue:

1. **Savant inbox sweep (keep this one).** The Ollama Kerri `AgentSchedule` inside `kerrihq-rails`, running on the Mac-mini worker (`com.kerrihq.kerri-worker`) / Render. Files `Reply: <Name> (<Company>)` cards via the `create_task` tool → `source_kind: manual`, no external_ref.

2. **Legacy codex/KerriOS inbox worker (the duplicate source).** launchd `com.kerri.inbox-worker` (every 600s) → `~/.kerri-chief/bin/kerri-inbox-worker.sh` → `~/.kerri-chief/runtime/src/cli.js worker sweep`. Files `[SEND APPROVAL] Reply to <Name> (<Company>)` cards via the `/api/v1/tasks` API → `source_kind: rails`, external_ref `benji-sweep-MMDD:Slug` (the "benji-inbox-sweep" lineage in `~/Projects/kerrios-agent-brain/agent-prompts/benji-inbox-sweep/`). Despite KerriOS being "retired" 2026-06-18, this is still live (cards dated 6/28 and 6/30; status log written 6/30 15:17 ET). It has a `skippedCoveredByKerri` field meant to defer to the Savant sweep, but it reads 0 — coverage detection is broken, so it does NOT dedup against Savant.

**Proof of the dup:** Quilter (Iryna) appears as both `Reply: Iryna Zhuravel (Quilter)` (Savant, manual) and `[SEND APPROVAL] Reply to Iryna (Quilter)` (benji-sweep-0628:QuilterBlur, rails).

**What I fixed (PR #223, merged + deployed + worker restarted):** `create_task` now dedups email-draft tasks by primary recipient — stops the *agent* from filing two cards for one email. This does NOT stop the legacy worker, which writes via the API and bypasses `create_task`.

**DONE 2026-06-30 (Brian: "retire it, Savant owns drafting").** `launchctl bootout` + plist moved to `~/Library/LaunchAgents/retired-2026-06-30/` for both `com.kerri.inbox-worker` (the drafter) and its companion `com.kerri.inbox-sweep-reaper`. To revive: move the plist back + `launchctl bootstrap gui/$(id -u) <plist>`. Liveness false-alarm suppressed: `kerri-inbox-sweep` set `monitored:false` in `kerrios-agent-brain/agent-prompts/routines.manifest.json` (commit f195af5). Savant worker `com.kerrihq.kerri-worker` is the surviving single drafter.

**Follow-through 2026-07-02 (Brian: "delete the agents we're no longer using").** The 14 legacy per-routine Agent records left behind by the retired launchd system kept `report_interval_hours` set, so `AgentHealthCheckJob` filed daily "Agent overdue" cards after reporting stopped 06-30. All 14 set to `status: retired` via a Render one-off job (run history preserved; hard delete blocked by `runs restrict_with_error`): kerri-inbox-sweep, kerri-brain-push, kerri-build-proposal, kerri-gap-sweep, kerri-pipeline-followup, kerri-renewal-watchdog, kerri-cold-outreach, kerri-eod-meetings-review, kerri-industry-intel, kerri-lead-research, kerri-morning-brief, kerri-morning-brief-retry, kerri-revenue-standup, kerri-self-improve. 7 open "Agent overdue" cards closed with completion proof. After: `Agent.overdue` = 0 and NO active agent carries a reporting interval, so the overdue-card generator is structurally silent. Un-retire by flipping status back to active.

**Legacy-only functions that LAPSED** (the old worker did these; Savant does not yet): inbox auto-archive of handled mail (`KERRI_ARCHIVE_COMPLETED_INBOX`), Apple Reminders "Kerri Approvals" reminders, and sendblue iMessage/text alerts. Drafting + approved-sends ARE covered by Savant (inbox sweep + `SendApprovedDraftsJob`). Port any of the three into Savant if Brian wants them back.

Related: [[kerrios_migration]], [[feedback_no_phantom_pipeline]].
