---
name: savant-approve-and-advance
description: "PR #311 rip-through reviews (approve lands on next card + j/k keys): MERGED + LIVE on prod, verified in Brian's browser 2026-07-02"
metadata: 
  node_type: memory
  type: project
  originSessionId: da22ff3a-3441-4a3a-8025-49807d3242ca
---

**Savant approve-and-advance + j/k navigation: PR #311 MERGED + DEPLOYED + VERIFIED ON PROD 2026-07-02** (merge commit da2e139, live on both Render services ~17:17 UTC).

Approve & Send / Mark Done / Skip on the task review page redirect to the next card in the same column (board order, same brand/lane/property filters, neighbor computed server-side before the status change); board fallback with "That column is clear." when empty. `j`/`k` keys hop next/prev card (`task_review_controller.js`); Space deliberately rejected (scrolls + typed in edit-draft textarea). `Task.ordered` gained an `id DESC` tie-break.

Verification: full suite 3093 examples 0 failures + headless-Chrome system spec locally; on prod (savant.kerrihq.com, Brian's real approval queue) confirmed j advances, k returns, next/prev URLs correct at column edges, J/K hint renders, and j inside the edit-draft textarea does not navigate. The approve-redirect itself was NOT clicked on prod (it sends real mail); it's covered by request + system specs of the deployed code.

Related: [[savant-quiet-console-redesign]] (builds on the merged quiet-console review page).
