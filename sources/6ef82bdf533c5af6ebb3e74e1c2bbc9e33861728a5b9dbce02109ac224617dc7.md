---
name: savant-hardening-2026-07
description: 2026-07-01 Savant hardening + agent-tree upgrade program — 19 PRs merged/deployed; PR
metadata: 
  node_type: memory
  type: project
  originSessionId: 0d208fbb-1366-427f-86eb-7e852301abb6
---

On 2026-07-01 a 20-package hardening + agent-tree upgrade program ran against kerrihq-rails via 5 waves of coding subagents. 19 PRs (#245-267, #275, #280 range) merged to main and deployed live on Render. Highlights: AgentFlowExecutor (durable approval-gated multi-step flows — `Agents::FlowExecutor`, `AgentFlowStepJob`, `resume_agent_flow` tool), Ollama retry/backoff + `ASK_SAVANT_MODEL_FALLBACKS` fallback chain (env unset = off; Brian must pick fallback models), tool error classifier + circuit breaker, confirmation-gate negation fix, run telemetry in `agent_runs.run_stats` + `ScheduledRunFailureAlertJob`, token-accurate compaction, delivered-vs-attempted delivery tracking, cascade-delete restriction (deleting company/agent with history now refuses loudly), send-path double-send fixes (approved drafts hold as `send_unconfirmed` instead of auto-resend), schema indexes/constraints. Eval suite went 7→10 golden cases, 10/10 twice on live glm-5.2.

**RESOLVED 2026-07-01 night: SendGrid RETIRED from the stack (Brian's decision).** #245 merged (webhook fail-closed, live) and then #284 merged (full retirement): ALL ActionMailer traffic (outreach, contracts, invitations, sponsor portal) now sends via `MailboxMailDelivery` -> MicrosoftGraphClient/GmailMailboxClient (fallback mailbox info@hardwarefyi.com via `MAILER_FALLBACK_MAILBOX`); SendGrid webhook controller/jobs/config removed; SENDGRID_* env vars auto-removed by Render blueprint sync; historical open/click data kept (counts stop accruing). Follow-ups: (1) DNS cleanup blocked — the kerrihq.com zone is NOT in Brian's Cloudflare account (his account only has ironcladmaritime.com + Pages); find who owns the zone (Benji?) and remove inbound/inboundemail MX + s1/s2._domainkey CNAMEs; hardwarefyi.com s1/s2._domainkey CNAMEs are on GoDaddy. Rationale: orphaned SendGrid account u54683090 can DKIM-sign as both domains until removed. (2) Reply-capture + open/click tracking now dormant; ProcessInboundEmailJob kept but unreachable — rebuild reply detection on a Graph/Gmail sweep if wanted. (3) A "Agent Lee needs a read-only API token" prompt appears in Brian's Cloudflare dashboard — flagged to Brian, not acted on.

**Historical note (superseded):** 2026-07-01 findings: a SendGrid account exists (ID u54683090; DKIM CNAMEs on hardwarefyi.com + kerrihq.com; `inbound.kerrihq.com` MX -> mx.sendgrid.net feeding Savant's inbound webhook), but it is registered to an email OUTSIDE all five KMG mailboxes (searched kerri@/brian@/info@ hfyi, brian@kerrihq Gmail, S&W Superhuman — zero SendGrid mail). Likely set up by a predecessor (Don?). The production `SENDGRID_API_KEY` is DEAD (401 on every endpoint incl. mail/send), so SendGrid SMTP outbound (OutreachMailer, contract-signing mailer) cannot currently work; approved drafts/replies are unaffected (they send via Graph/Gmail). Prod webhook remains unauthenticated until #245 merges with a secret. Brian must choose: recover the account (find owner), rebuild fresh under brian@kerrihq.com, or retire SendGrid from the stack (then #245 can merge fail-closed immediately). Render API key: `~/.kerri-chief/secrets/render.env`; services srv-d8kvn767r5hc739fjo9g (web), srv-d8kvnkcvikkc73c8cm6g (worker). Cloudflare API (`cloudflare.env`) can edit the DNS if rebuilding/retiring.

Model fallback chain is SET and live (2026-07-01, Brian-approved): `ASK_SAVANT_MODEL_FALLBACKS=glm-5.1,deepseek-v4-pro` on both Render services (glm-5.1 = same-family behavioral match; deepseek-v4-pro = second line for a GLM-family outage). Engages only when glm-5.2 exhausts retries on retryable errors.

Other optional owner items: confirm `DEFAULT_INBOUND_EMAIL_ORG_NAME` env on Render; optionally enable SendGrid Signed Event Webhook later (`SENDGRID_EVENT_WEBHOOK_PUBLIC_KEY`). Live eval runs work locally by sourcing `~/.kerri-chief/secrets/ollama.env` (watch the session quota — 429s poison runs). Related: [[kerrihq-rails-dev-env]], [[agent-eval-harness]].
