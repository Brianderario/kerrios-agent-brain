---
name: hwfyi_sponsor_placement_perf
description: Where Hardware FYI newsletter ad placement performance (primary vs Tools slot) lives + how to re-pull the numbers
metadata: 
  node_type: memory
  type: reference
  originSessionId: 260a1e94-ec57-42e0-bd31-7c376b23b4d2
---

Hardware FYI sponsor **ad placement performance** (by placement, not by client) is staged in Savant `/brain`: KnowledgeRecord **id `82e4579c-7ab9-4d7e-ab0a-3cc70e5a43dc`**, title "Hardware FYI — Sponsor Ad Placement Performance", domain `hardware_fyi`, kind `operational_doc`, source `hwfyi_advertising_tracker`, source_path `analytics/sponsor-placement-performance.md`. Staged as `candidate` 2026-06-25 (Brian can promote to canonical in /brain). Re-push via `POST /api/v1/knowledge_record_imports` (idempotent on source_path via content_hash; use `KERRIHQ_BRAIN_IMPORT_KEY`).

Headline (May 26–Jun 23 2026 snapshot): **Primary placement ~18 clicks/issue (range 9–32); one Tools-section slot ~5 (range 2–8)** — primary ≈ 3–4x a Tools slot. Key caveat: Substack only reports per-link clicks above a per-issue floor (~6–10); below-floor links return no data (not zero), so Tools per-slot is an estimate.

**Refresh recipe** (re-pull at will): Substack publisher dashboard for hardwarefyi.substack.com, same-origin authed fetches — `GET /api/v1/post_management/published` (IDs), `/api/v1/drafts/{id}/stats` (sent/opens/clicks), `/api/v1/drafts/{id}/links?offset=N` (paginate by 10, dedupe by raw URL, `has_more_links` is unreliable), `/api/v1/posts/by-id/{id}` (body_html for placement mapping). Drive the signed-in browser via the Chrome extension MCP (Claude_in_Chrome), not AppleScript (Apple-Events JS is off). Working files: `~/Projects/hwfyi-sponsor-reports/`. See [[hwfyi_crm_sheet]], [[feedback_sales_value_first]].
