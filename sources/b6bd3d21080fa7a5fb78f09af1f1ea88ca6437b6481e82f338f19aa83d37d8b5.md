---
name: kerri-company-assets-history-tools
description: "PR #302: Kerri's save_company_asset/list_company_assets (sponsor files on company pages, Postgres-stored bytes) + search_slack_history/read_slack_history (message recall) + skills = AgentProgram standing orders"
metadata: 
  node_type: memory
  type: project
  originSessionId: 08c58a8c-a47b-4614-94de-dc34c1b7258d
---

PR kerrihq/kerrihq-rails#302 — MERGED + DEPLOYED to prod 2026-07-02 (verified live: new download route answers on savant.kerrihq.com). Built in worktree `~/Projects/kerrihq-rails-sponsor-assets` (branch deleted on GitHub; worktree can be removed). Gives the Savant Slack agent capabilities Brian asked for:

1. **Sponsor assets on company pages**: `save_company_asset` files a Slack-uploaded file (attachment blocks now expose `slack_file_url`), Drive file, external URL, ad copy, or CTA onto a Company, optionally tied to a Deal (partner-program ads). Bytes live in Postgres (`sponsor_assets.file_data`, 15 MB cap) because Render prod has NO S3 and web/worker have separate ephemeral disks — disk ActiveStorage is unservable/wiped there (same latent bug affects `Contract#signed_document`, spawned as separate task 2026-07-02). Company page renders a "Files & Assets" section; download via `GET /organizations/:org/sponsor_assets/:id/download`.
2. **Message-history recall**: `search_slack_history` (keyword, ~1,200 msgs / up to 365 days back) + `read_slack_history` (window / before_ts paging / thread_ts) via each workspace's bot token — works for Kerri, Oliver, Vaughn. Default = current conversation; retrieval on demand instead of holding context.

`SponsorAsset.sponsor_commitment` is now optional (needs commitment OR company anchor); reviewer notifications only fire for commitment uploads.

**Follow-up PR #305 (merged 2026-07-02, commit 3d50704):** save_company_asset also accepts EMAIL attachments — `mailbox` + `message_id` + `attachment_name` (or attachment_id), via new `download_attachment` on MicrosoftGraphClient + GmailMailboxClient (id or partial-name match, size-checked before fetch, same permission gate as read_mail). Prompt sentence is mail-tools-gated so Oliver (mail-off) never sees it. Gap surfaced by Kerri's first real use: sponsor logos arrive as Outlook attachments.

3. **Skills for repeat patterns** (commit 2 on the PR): the harness ALREADY had a full skills system — `AgentProgram` (standing orders: name/triggers/instructions) + `AskSavant::StandingOrdersContext`, which keyword-matches active programs against each inbound message and auto-injects the top 3 into the turn. Kerri's "skills" ARE agent programs; PR #302 just taught her (system prompt SKILLS rule + tool descriptions) to save one when asked to learn a skill or when a task/correction repeats. Don't build a separate skill model.

**Follow-up PR #307 (merged + live 2026-07-02, commit 5589f2f):** `send_slack_message` takes a `workspace` param (team id/config key) so Kerri posts into her OTHER install (HWFYI T08A2U02GHJ ↔ KerriHQ T09UJ7NHRK2) — same-assistant installs only, never Oliver/Vaughn; channel-not-found errors list her other installs. Plus `archive_company_asset` (hides duplicate/stale assets from the company page; list + page exclude archived by default). Verified live: cross-workspace confirmation to Benji landed in HWFYI C0BCMHA5C13. Local `~/.kerri-chief/secrets/kerri-slack-bot.env` holds bot tokens for BOTH workspaces (useful for verifying her posts in HWFYI, which Brian's Slack MCP can't see).

**Deploy-restart stall (2026-07-02):** a Render deploy that lands while Kerri is mid-turn kills the turn and can leave the Slack session stuck `active` (dead AgentTask blocks the queue). PR #304's recovery only fires on the NEXT inbound event AND only if Solid Queue already logged ProcessPrunedError — a too-early follow-up message queues forever. Remedy: send any new message to the DM after a few minutes (triggers `SessionQueue#schedule_if_idle` → release); diagnose via `GET /api/v1/agent_runtime_health` (sessions + tasks) with `KERRIHQ_AGENT_API_KEY` from `~/.kerri-chief/secrets/kerrihq.env`; Render deploy timeline via `RENDER_API_KEY` in `render.env` (worker srv-d8kvnkcvikkc73c8cm6g, web srv-d8kvn767r5hc739fjo9g). Hardening spawned as a task; WIP branch fix/slack-session-pruned-recovery in the main checkout.

**Worktree/testing lessons:** fresh kerrihq-rails worktrees need `vendor/bundle` symlink + `.bundle/config` copy + `app/assets/builds` copy from the main checkout (system specs fail with fake overflow without built CSS). Parallel sessions share the Postgres test DB and reset its schema under each other — run big suites against an isolated DB: `DATABASE_URL=postgres://localhost/kerrihq_test_sponsor_assets RAILS_ENV=test bundle exec rspec` (db created 2026-07-02). See [[kerrihq-rails-dev-env]].
