---
name: superhuman-mcp-savant
description: "Superhuman Mail MCP wired into the Savant/Kerri Ollama agent for Brian's S&W mailbox — PR #194, Brian-only, awaiting merge+OAuth connect"
metadata: 
  node_type: memory
  type: project
  originSessionId: 14f25304-61be-48bd-87e3-6e0dedca2997
---

Wired Brian's **Standard & Works mailbox** into the Savant agent (the Ollama `glm-5.2` agent behind the Kerri/Oliver Slack bots — see [[slack_agents_architecture]]) over Superhuman's remote MCP, mirroring the existing **Reclaim** OAuth/MCP integration in `kerrihq-rails`. Shipped as [kerrihq-rails#194](https://github.com/kerrihq/kerrihq-rails/pull/194) (branch `feat/superhuman-mcp`, worktree `~/Projects/kerrihq-rails-superhuman`).

**The Savant agent already speaks MCP** — `ReclaimMcpClient`/`ReclaimOauthClient`/`ReclaimClient` + an OAuth controller storing encrypted tokens in `IntegrationConnection`. Adding an MCP server = cloning that pattern, NOT a config toggle and NOT `claude mcp add` (that agent isn't Claude Code).

New code: `SuperhumanOauthClient` (PKCE + dynamic client registration at `mcp.auth.mail.superhuman.com`, public client, scopes `openid email profile offline_access`, RFC 8707 resource indicator), `SuperhumanMcpClient` (streamable-HTTP JSON-RPC to `mcp.mail.superhuman.com/mcp`, Bearer = OIDC **id_token** with access_token fallback — server 401s `missing-id-token` without it), `SuperhumanClient`, `SuperhumanOauthController` + routes + Integrations connect panel, and AskSavant tools `list_superhuman_capabilities`/`call_superhuman_tool`. `IntegrationConnection.provider` gained `:superhuman` (integer enum, no migration).

**Gating (Brian's decisions):** Brian-only + KerriHQ Slack workspace only (`AccessPolicy.superhuman_email?` + `kerrihq_slack_workspace?`) so the S/W boundary holds; full capability incl. send, but every mutation is preview→`confirm:true` + audited; prompt tells Kerri never to copy S&W mail into the brain.

Verified: 43 new specs + 183 adjacent (AskSavant/integrations/Reclaim) green, rubocop/brakeman clean, live DCR registration confirmed against the real Superhuman server.

**Remaining (Brian):** merge #194 + deploy (Render, branch `main`), then one-time browser sign-in to the **standardandworks.com** Superhuman account via Integrations → Superhuman → Connect with Superhuman to store the OAuth tokens. Needs the S&W Superhuman account on Business/Enterprise + Ask AI (Brian confirmed). I did NOT merge/deploy — his call.
