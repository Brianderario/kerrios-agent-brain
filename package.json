{
  "name": "kerrios",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sanitized KerriOS company brain for Kerri Media Group agents.",
  "scripts": {
    "export:agent-seed": "node scripts/export-agent-seed.mjs",
    "check": "find scripts test -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n 1 node --check",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
