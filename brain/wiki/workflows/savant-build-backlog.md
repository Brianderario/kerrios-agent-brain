# Savant Build Backlog

scope: workflow · updated: 2026-06-26 · owner: Brian + Kerri

The living queue of Savant product improvements. This is the memory that stops the nightly [[../../../agent-prompts/kerri-build-proposal/SKILL.md]] routine from re-proposing the same thing: it reconciles shipped items off the list, never proposes a retired/declined slice, and pulls the next undone candidate. Brian and Kerri groom this together; Brian can add ideas any time. Grounded in [[../properties/savant-product-vision]] (the ten pillars).

**How the nightly routine uses this page:** read the Candidates table, drop anything now Shipped/Retired (reconcile against the latest `data/build-loop-runs/` ledger), skip anything already `proposed` in the last 7 days that Brian has not actioned, pick the highest-value remaining Overnight-safe candidate, then mark it `proposed <date>`. When a candidate ships, move it to Shipped with its date + commit. When Brian declines one, move it to Retired/Declined with the reason.

## Permanent exclusions (never propose)

| Item | Why excluded |
|------|--------------|
| `$1M revenue_command` screen + `GET /api/v1/revenue_command` | **Retired by design 2026-06-14** (Brian confirmed the removal was intentional). Source live booked/gap from `/api/v1/deals` + `/api/v1/renewals` instead; never rebuild this surface or endpoint. |

## Shipped (reconcile OFF the candidate list; here so the routine remembers)

| Item | Pillar | Shipped |
|------|--------|---------|
| Renewals worklist + `GET /api/v1/renewals` + renewal_brief + case files | 5/8 | 2026-06-14 |
| Owed-deliverables roll-up (contracted vs delivered vs owed) | 1/5 | 2026-06-13 |
| Newsletter sell-through + editable placements (assign/schedule slots) | 1 | 2026-06-12 |
| Sponsor dashboard / proof-of-performance framework | 5 | 2026-06-12 |
| CRM relationship view (company→deals+people, person→deals) | - | 2026-06-13 |
| Front-page Overview redesign (NEEDS YOU queue + revenue band) | 6/9 | 2026-06-12 |
| Mobile nav drawer + PWA (Savant on the phone) | - | 2026-06-12 |
| Brain hub migration: full coverage, read-flip, write-flip, operating-layer, git-demote (Phases 1-5) | - | 2026-06-17 |
| Gap-closing pipeline: GET /api/v1/deals/:id/deal_brief (Crm::DealBrief) + Pipeline worklist screen under Revenue | 4/5 | 2026-06-18 (79e9395, 790424b) |
| PWA installability (active service worker, app shortcuts, safe offline) + mobile bottom nav for daily lanes | - | 2026-06-18 |
| Monaco redesign Phase 1: foundation (tokens/serif/pill component classes/shell) + Overview reskin | 6/9 | 2026-06-18 (d4ddfda) |
| Monaco look across all core screens (carried cohesively by the foundation; Tasks/Pipeline/Renewals/Revenue/Companies/kanban) + pipeline deal-card drag fix | 6/9 | 2026-06-18 (76b520b; 6/18 triage: remaining = optional editorial polish) |
| Ask Savant in-app copilot — slice 1 (read-only tools: pipeline_summary/find_deals/list_tasks/search_brain) on Ollama glm-5.2, global drawer from the Overview ask bar | 6 | 2026-06-18→19 (a4abee8, 42ce4c3) |
| Multi-user access matrix: capability helpers + restricted-event flag, strict per-user task siloing, pipeline gated to full members, Ironclad restricted to allow-list, Ari/Benji/Zach provisioned (Zach=event_guest), Ask Savant reads scoped per user — all prod-verified | - | 2026-06-19 (635c35e…aa39801, 8de18c5) |
| Ask Savant in-app ACTIONS — slice 2 (update_deal preview→confirm, create_task/update_task self-scoped, each Pundit-authorized as current_user + AgentRun audit, no external sends) | 6 | 2026-06-19 (98df743) |
| Savant queue-health badge clickable (failing-check popover) + Person `partner` contact_type (closes the recurring inbox-sweep enum gap) | - | 2026-06-19 (2ae39b7 #122, a49962b #121) |
| **Pricing intelligence read view** (candidate #1) — Revenue → Pricing intelligence: win/loss + win rate + realized price range by deal category and deal-size band, real closed deals only, full-members only | 7 | 2026-06-20 (0dff010, 2160915) |
| Dashboard calm-down + revenue quick-view (Brian-directed 6/19-20): revenue/forecast/won/$1M-gap strip on the deals board + Pipeline worklist (filter-aware), Overview event cards collapsed to a single summary line via `<details>` | 6/9 | 2026-06-20 (5c5e7ba, e9d7fb7, 216c177, 4448fee) |
| **Signal feed / prioritized prospect worklist** (candidate #2) — Revenue nav: companies ranked by recent `account_signal` activity (Crm::SignalFeed service + `/signals` screen), each row linking to company + signals, Pundit-scoped, full-members only, real signals only | 3 | 2026-06-21 (aa3f21f, b0f8619) |
| **Event/Kinetic sponsorship yield view** (candidate #4) — event-show panel: per-tier sold/reserved/realized-vs-default $ from real deal+event_sponsor state (`Events::SponsorshipYield` service), $ gated behind `can_see_pipeline?`, no invented capacity | 8 | 2026-06-24 (b099adb via PR #135 "Full Savant console QA hardening"; the 6/22 build-loop branch's PR #131 was closed once the work re-landed inside #135 — files `app/services/events/sponsorship_yield.rb` + `_sponsorship_yield.html.erb` confirmed on origin/main) |

## Directed active work — Monaco redesign (Brian directive 2026-06-18, takes priority)

Brian directed the build loop on 2026-06-18: "Execute the Monaco-inspired design (pure-black canvas, editorial serif display, floating pill nav, monochrome + violet→magenta accent, calm morning-brief home) across ALL of Savant. Keep the DATA whole; change the LAYOUT. Then wire a Claude SDK agent ('Ask Savant'). Run the loop until Savant looks like the future of a media company's OS." This is presentation-only (additive, view-layer, no migrations/API/sends) so it is overnight-safe AND it is Brian's standing same-day instruction — it outranks the generic candidates below until the screens are done. Foundation + Overview shipped 2026-06-18 (d4ddfda).

**SUBSTANTIALLY COMPLETE as of 2026-06-19.** The 6/18 triage found the foundation carried the Monaco look across every core screen cohesively, so the remaining reskin rows are optional editorial polish (deferred). The Ask Savant agent shipped both slices (read 6/18, actions 6/19) on Ollama glm-5.2, and the 6/19 run added the full multi-user access matrix (prod-verified). With the directed work done, the nightly routine now pulls from the generic Overnight-safe queue below until Brian directs fresh same-day work.

| Slice | Screens | Status |
|-------|---------|--------|
| Daily operating lanes | Tasks board, Approvals + Approval requests, Pipeline worklist + Deals kanban | **DONE** — absorbed by the foundation (6/18 triage); kanban drag fixed (76b520b); remaining = optional polish |
| CRM + knowledge | Companies + People, Knowledge records (Brain) | optional polish (foundation look already applied) |
| Revenue + content | Renewals + Revenue, Newsletter inventory + issues | optional polish (foundation look already applied) |
| Events + chrome | Events + Sponsor portal, Command palette + mobile bottom-nav polish + login screen | optional polish (foundation look already applied) |
| Ask Savant agent (Claude SDK) | additive read-only endpoint grounded in knowledge_records + CRM, streamed into the ask bar; any action approval-gated | **SHIPPED** — slice 1 read (a4abee8, Ollama 42ce4c3) + slice 2 actions scoped/audited (98df743) |

## Overnight-safe candidates (the queue — pull the top undone one when no directed work is open)

Ranked revenue/leverage first. All are additive, internal, real-data-only (phantom-pipeline rule), inside existing Savant nav, no external sends — i.e. safe for the unattended loop. Verified undone as of 2026-06-17. The directed Monaco/agent work above is now complete (2026-06-19), so this is the live queue: pull the top undone one each night.

| # | Candidate | Pillar | Value / revenue tie | Night-sized "done" | Status |
|---|-----------|--------|---------------------|--------------------|--------|
| ~~1~~ | ~~**Pricing intelligence read view**~~ | 7 | SHIPPED 2026-06-20 (run #2, 0dff010+2160915) → moved to Shipped table. | — | **SHIPPED 2026-06-20** |
| ~~2~~ | ~~**Signal feed / prioritized prospect worklist**~~ | 3 | SHIPPED 2026-06-21 (run, aa3f21f Crm::SignalFeed service + b0f8619 `/signals` screen) → moved to Shipped table. | — | **SHIPPED 2026-06-21** |
| 3 | **Touches-per-dollar / deal activity metric** | 4 | The efficiency number the vision manages by; shows which deals are over-worked vs neglected. | Deal show page + deals index show a touch count (from `activity`/outreach records) and, on closed-won, touches-per-dollar; real activity only. | **offered as "touches" pivot 2026-06-28** (was last night's 6/27 primary, so rotated OFF tonight per the two-nights-running rule; still undone — grep `touches_per_dollar`/`touch_count`/`deal_activity` on origin/main = 0. Data confirmed present: deals API exposes `engagement_signal_count`/`last_signal_at`/`last_activity_at`/`stakeholder_count`. Prior: proposed 6/21, 6/25, 6/27) |
| ~~4~~ | ~~**Event/Kinetic sponsorship yield view**~~ | 8 | SHIPPED 2026-06-24 → moved to Shipped table. The 6/22→23 build-loop branch could not deploy (classifier blocked the push) and sat in PR #131; the work re-landed on `origin/main` inside PR #135 "Full Savant console QA hardening" (b099adb) on 2026-06-24, so PR #131 was closed. Files confirmed on main. | — | **SHIPPED 2026-06-24** |
| 5 | **Product pulse report (read-side)** | 6 | A time-windowed "what actually happened" report (sends, deals moved, approvals cleared, agent runs) so the next week anchors to real signal. | A read view summarizing a chosen window (7d/30d) from existing records; saved nowhere new, just a screen. | **proposed 2026-06-28** (rotated back as tonight's primary because #3 was last night's 6/27 primary and the rule forbids the identical pick two nights running; #5 last primary 6/26, not pitched last night; still undone — grep `product_pulse`/`pulse_report`/`ProductPulse` on origin/main = 0. Offered #3 as the one-word "touches" pivot. Prior: proposed 6/23, carried 6/24/6/25, primary 6/26) |
| H1 | **Empty-state polish on the three revenue report screens** (Pricing intelligence / Signal feed / Sponsorship yield panel) | 6 | Hygiene: each renders a blank/zero table at no-data; clear empty states make them read at zero data. Proposed only because the novel queue is dry. | Per-screen empty state + empty-case spec, real data unchanged, $ gating still proven on the yield panel; view-layer only, no API/migration/sends. | **proposed 2026-06-24** (dry-queue hygiene default; one-word "go") |

## Supervised projects (NOT for the overnight loop)

Too big, or need external data / paid credits / external sends, which the build-loop hard rails forbid. Build these with Brian in a normal session.

| Project | Pillar | Why not overnight |
|---------|--------|-------------------|
| **Audience graph / subscriber moat** | 2 | The biggest strategic gap, but needs beehiiv subscriber ingestion + Apollo enrichment (external data + paid credits). Scoped separately in [[audience-graph-project]]. |
| Signal-driven autonomous outbound (send path) | 3/4 | Involves external sends; overnight runs never send. The signal *feed* (candidate #2) is the safe overnight half. |

## Grooming log

- 2026-06-28 — Reconciled against `origin/main` (fetched; HEAD **1268fbc**). New since the 6/27 reconcile point (2337397): **12 commits** (ef7f892…1268fbc) — Ollama num_ctx/context-compaction tuning, scheduled AskSavant tasks (#176), AskSavant context-compaction + code-shipping hardening (#177), Slack deploy PR action-loop fix (#178), memory-policy routing + create_person tool, native workspace runs + web research, Parallel API key in encrypted creds, Stagehand browser runs in AskSavant, Oliver Slack app support — **all interactive Savant-agent harness / Slack / browser-automation infra, NONE from the Overnight-safe queue**, so nothing new shipped overnight. Live undone Overnight-safe queue unchanged = **#3 touches-per-dollar** + **#5 product pulse** (+ H1 hygiene); both reconfirmed still undone — `git grep -lE touches_per_dollar|touch_count|deal_activity|product_pulse|pulse_report|ProductPulse` on `origin/main -- app/` = **0 matches**. **The novel queue is genuinely dry** (everything proposed within the last 7 nights: 6/22 #4, 6/23 #5, 6/24 H1, 6/25 #3, 6/26 #5, 6/27 #3). Last night (6/27) the primary was **#3 touches-per-dollar**, so per "never the identical thing two nights running" I rotated the primary OFF #3 and **PROPOSED #5 Product pulse report (read-side)** (last primary 6/26, not pitched last night; the highest-value remaining revenue-analytics read, pure read-only view-layer beside the shipped Pricing-intelligence / Signal-feed report screens; $ figures gated `can_see_pipeline?`). Offered **#3 as a one-word "touches" pivot** and **H1 as "hygiene"**, and told Brian plainly the novel queue is exhausted — green-light one of the three ready slices, feed fresh ideas, or pull the supervised audience-graph project into a real session. No new build candidate from open Console cards (build-keyword scan of the 100-card open list = only routine/inbox-sweep/pipeline/venue cards + the interactive Slack-harness "🌙 OVERNIGHT BUILD REPORT 2026-06-27", no Savant product slice). Live revenue (paginated /api/v1/deals = **124 deals** + /api/v1/renewals): **booked $668,422.41 (43 won) / gap $331,577.59** (flat vs 6/24-6/27, no new close), open pipeline 73 = $207,500, late-stage 13 = $192,500 (10 proposal_sent $143.5K [PTC $50K / Summit $25K / CoLab $15K / Viam $12.5K / Alta $10K / Neural Concept $10K / Almond $6K / GrayMatter $5K / Rev1 $5K / MFG Flow $5K] + Duro contract_sent $44K + Modelwise contract_sent $5K + Xometry negotiation $0), renewals 5 = $41,758.16.
- 2026-06-27 — Reconciled against `origin/main` (fetched; HEAD **2337397**). New since the 6/26 reconcile point (9b48fab): a run of **AccessPolicy-module extraction / ToolExecutor hash-table dispatch / durable Slack socket ack / code-tool deferral + green-PR-merge** commits (#162…#170, 2337397) — all interactive Savant-agent harness + access-policy infra (matches tonight's open Console card "🌙 OVERNIGHT BUILD REPORT — Slack agent rebuilt into a Codex-class harness"), **NONE from the Overnight-safe queue**, so nothing new shipped overnight. Live undone Overnight-safe queue unchanged = **#3 touches-per-dollar** + **#5 product pulse** (+ H1 hygiene); both reconfirmed still undone — `git grep -lE touches_per_dollar|touch_count|deal_activity|product_pulse|pulse_report|ProductPulse` on `origin/main -- app/` = **0 matches**. **The novel queue is genuinely dry** (everything proposed within the last 7 nights: 6/21 #3, 6/22 #4, 6/23 #5, 6/24 H1, 6/25 #3, 6/26 #5). Last night (6/26) the primary was **#5 product pulse**, so per "never the identical thing two nights running" I rotated the primary OFF #5 and **PROPOSED #3 Touches-per-dollar / deal activity metric** (last primary 6/25, not pitched last night; highest-value undone revenue-analytics read, pure read-only view-layer — backing data already on the deals API: `engagement_signal_count`/`last_signal_at`/`last_activity_at`/`stakeholder_count`). Offered **#5 as a one-word "pulse" pivot** and **H1 as "hygiene"**, and told Brian plainly the novel queue is exhausted — green-light one of the three ready slices, feed fresh ideas, or pull the supervised audience-graph project into a real session. No new build candidate from open Console cards (build-keyword scan of the 100-card open list = only the interactive Slack-harness build report + a NOW.md apply-or-archive hygiene recommendation, no Savant product slice). Live revenue (paginated /api/v1/deals = **124 deals** + /api/v1/renewals): **booked $668,422.41 (43 won) / gap $331,577.59** (flat vs 6/24-6/26, no new close), open pipeline 73 = $207,500, late-stage 13 = $192,500 (10 proposal_sent $143.5K [PTC $50K / Summit $25K / CoLab $15K / Viam $12.5K / Alta $10K / Neural Concept $10K / Almond $6K / GrayMatter $5K / Rev1 $5K / MFG Flow $5K] + Duro contract_sent $44K + Modelwise contract_sent $5K [moved proposal_sent→contract_sent since 6/26] + Xometry negotiation $0), renewals 5 = $41,758.16. (Env note: `kerrihq.env` uses bare `KEY=value`, so `source` does NOT export to a `node` child — use `set -a; source; set +a` or curl inline, else the deals/renewals pull silently reads as Unauthorized.)
- 2026-06-26 — Reconciled against `origin/main` (fetched; HEAD **9b48fab**). New since the 6/25 reconcile point (a15a832): a run of **Slack digital-worker / Ollama / Reclaim+Google OAuth / agent-memory-lane / direct-code-shipping** commits (8646af3…9b48fab) — all interactive Savant-agent infra, **NONE from the Overnight-safe queue**, so nothing new shipped overnight (last build loop was still 6/22→23 sponsorship-yield, landed inside #135 on 6/24). Live undone Overnight-safe queue unchanged = **#3 touches-per-dollar** + **#5 product pulse**; both reconfirmed still undone — grep `touches_per_dollar`/`touch_count`/`deal_activity`/`product_pulse`/`pulse_report`/`ProductPulse` on the working tree `app/` = **0**. **The novel queue is genuinely dry** (everything proposed within the last 7 nights: 6/18 lanes, 6/19 #1, 6/20 #2, 6/21 #3, 6/22 #4, 6/23 #5, 6/24 H1, 6/25 #3). Last night (6/25) the primary was **#3 touches-per-dollar**, so per "never the identical thing two nights running" I rotated the primary OFF #3 and **PROPOSED #5 Product pulse report (read-side)** — last pitched 6/23, not pitched the last two nights, built-ready, the highest-value remaining revenue-analytics read. Offered **#3 as a one-word "touches" pivot** and told Brian plainly the novel queue is exhausted: all three ready slices (#3/#5/H1) are scoped and waiting on a one-word go, he should green-light one or feed fresh ideas (or greenlight pulling the supervised audience-graph project into a real session). No new build candidates from open Console cards (build-keyword scan of the 100-card open list = only a newsletter Savant<>Sheet ops item + a cold-outreach second-touch batch, no Savant product slice). Live revenue (paginated /api/v1/deals = **124 deals** + /api/v1/renewals; API shape note: deals come back under `data[]` with a `value` field): **booked $668,422.41 (43 won) / gap $331,577.59** (flat vs 6/24-6/25, no new close), open pipeline 73 = $207,500, late-stage 13 = $192,500 (10 proposal_sent $143.5K [PTC $50K / Summit $25K / CoLab $15K / Viam $12.5K / Alta $10K / Neural Concept $10K / Almond $6K / GrayMatter $5K / Rev1 $5K / MFG Flow $5K] + Duro + Modelwise contract_sent $49K + Xometry negotiation $0), renewals 5 = $41,758.16.
- 2026-06-25 — Reconciled against `origin/main` (HEAD **a15a832**). New since the 6/24 reconcile point (f6e81cc): **#140 Brain reorg + ⌘K, #141 brain-importer fix, #142 Console-wide search behind ⌘K + Savant star favicon** — all interactive/QA console work, **NONE from the Overnight-safe queue**, so nothing new shipped overnight (the last build loop was 6/22→23 sponsorship-yield, which landed inside #135 on 6/24). Live undone Overnight-safe queue unchanged = **#3 touches-per-dollar (last proposed 6/21)** + **#5 product pulse (last proposed 6/23)**; both confirmed still undone — grep `touches_per_dollar`/`touch_count`/`deal_activity`/`product_pulse`/`pulse_report`/`ProductPulse` on origin/main `app/` all **= 0**. **The novel queue is genuinely dry** (everything proposed within the last 7 nights: 6/18 lanes, 6/19 #1, 6/20 #2, 6/21 #3, 6/22 #4, 6/23 #5, 6/24 H1). Last night ran the H1 hygiene default, so per "never the identical thing two nights running" I did NOT repeat it. **Rotated the primary pick back to #3 touches-per-dollar** — the highest-value undone candidate (pillar 4 efficiency/revenue lens), not pitched last night, and the backing data is already exposed at the deals API (`engagement_signal_count`/`last_signal_at`/`last_activity_at`/`stakeholder_count`), making it a pure read-only view-layer slice. Offered **#5 as a one-word "pulse" pivot** and told Brian plainly the overnight queue needs fresh ideas from him after #3/#5. No new build candidates from open Console cards (100 open cards reviewed — all inbox-sweep/pipeline/EOD/venue/finance, no Savant product slice). Live revenue (paginated /api/v1/deals = **124 deals** + /api/v1/renewals): **booked $668,422.41 (43 won) / gap $331,577.59** (flat vs 6/24, no new close), open pipeline 73 = $207,500, late-stage 13 = $192,500 (10 proposal_sent $143.5K + Duro + Modelwise contract_sent $49K + Xometry negotiation $0; Modelwise moved negotiation→contract_sent since 6/24), renewals 5 = $41,758.16.
- 2026-06-24 — Reconciled against `origin/main` (HEAD f6e81cc). **#4 Event/Kinetic sponsorship yield view SHIPPED 2026-06-24** → moved to Shipped. Its 6/22→23 build-loop branch never deployed (classifier blocked the push, sat in PR #131); the work re-landed on main inside **PR #135 "Full Savant console QA hardening" (b099adb)** on 6/24, so PR #131 was CLOSED (superseded, not merged). Confirmed `app/services/events/sponsorship_yield.rb` + `_sponsorship_yield.html.erb` + 2 spec files exist on `origin/main` (cat-file -e on `origin/main:` = YES). Also landed on main since the 6/23 reconcile: #129 durable signed-contract PDFs, #135 QA hardening, #136 per-user light mode, #137/#138/#139 task-routing fixes — all interactive/QA work, NOT overnight-queue candidates. **With #4 off, the live undone Overnight-safe queue = #3 touches-per-dollar (proposed 6/21) + #5 product pulse (proposed 6/23). BOTH fail the strict 7-night novelty rule, and #5 can't go two nights running → the novel queue is genuinely DRY** (exactly what the 6/23 entry predicted). Per STEP 2D, **PROPOSED a small novel hygiene slice (H1: empty-state polish on the three shipped revenue report screens — Pricing intelligence / Signal feed / Sponsorship yield)** as the runnable default, surfaced the real pending decision per the constraint line (the two scoped reads #3/#5 are ready and waiting on a one-word go, offered as one-word "touches"/"pulse" options), and asked Brian to feed fresh ideas because the novel queue is empty. Novelty of H1 confirmed: never proposed before; greps `product_pulse`/`touches_per_dollar`/`touch_count`/`deal_activity` on main all EMPTY (so #3/#5 also still genuinely undone). No new build candidates from open Console cards (tonight's list = H0189 Viam reply done + Astranis Ramp vendor portal [Brian-only finance]; all open 💡 are inbox-sweep/routine hardening, not Savant product slices). Live revenue (paginated /api/v1/deals = 122 deals + /api/v1/renewals): **booked $668,422 (43 won) / gap $331,578** (flat vs 6/23, no new close), open pipeline 71 = $207,500, late-stage 13 = $202,500 (11 proposal_sent + Duro contract_sent + 1 negotiation), renewals 5 = $41,758. **Queue note: the Overnight-safe queue is dry — Brian needs to green-light #3 or #5 (both built-ready) or feed fresh ideas; the hygiene default keeps the loop productive in the meantime.**
- 2026-06-23 — Reconciled the 6/22→23 overnight loop (NOW.md baton + `kerrihq-rails` `main` HEAD 77f1de1). **#4 Event/Kinetic sponsorship yield view was green-lit and BUILT overnight** (`Events::SponsorshipYield` service + event-show "Sponsorship Yield" panel, 12 specs, rubocop/brakeman clean) but the build-loop auto-mode classifier blocked the direct deploy, so it landed in **PR #131** and is NOT yet on `main` (HEAD shows #129/#130 only) → marked #4 "BUILT, in PR #131 awaiting merge", reconcile to Shipped once Brian merges. With #4 off the live queue, the remaining Overnight-safe candidates are **#3 touches-per-dollar (proposed 6/21, undone)** and **#5 product pulse (never proposed)**. Per the strict novelty rule (slice must not have been proposed in the last 7 nights), #3 fails (6/21, within 7) and #5 is clean → **PROPOSED #5 Product pulse report (read-side)** for tonight. Novelty + undone confirmed: codebase grep returns NOTHING for `product_pulse|pulse_report|ProductPulse`; it would sit beside the shipped `pricing_intelligence` / `signal_feed` / `newsletter_sell_through` report screens. Scoped it real-data-only, additive view-layer, full-members gate on $ (`can_see_pipeline?`): a `Reports::ProductPulse` service aggregating a 7d/30d window from existing records (deals advanced + closed_won $, approvals cleared, AgentRun by routine, placements run, new signals, renewals) + a "Product pulse" screen under Revenue nav. No new build candidates from open Console cards (tonight's 22:50 sweep confirmed all open 💡 are inbox-sweep/EOD/SKILL.md routine hardening, not Savant product slices). **Queue note: after #5 ships, every Overnight-safe candidate (#3/#4/#5) will have been proposed or built within the last week — flagged to Brian that the queue is dry and he should green-light the proposed-but-unbuilt ones or feed fresh ideas.** Live revenue (paginated /api/v1/deals = 109 deals + /api/v1/renewals): **booked $668,422 (42 won) / gap $331,578**, open pipeline 59 = $207,500, late-stage 12 = $190,000 (10 proposal_sent $141K + Duro contract_sent $44K + 1 negotiation $5K), renewals 5 = $41,758 (flat vs 6/22, no new close).
- 2026-06-22 — Reconciled the 6/21 ledger + `origin/main` (HEAD = b0f8619/aa3f21f, the Signal Feed commits; **no commits after them** = the 6/21-proposed #3 was never green-lit/run, so nothing new shipped). #2 Signal Feed already in Shipped. Per the novelty rule (never pitch the identical thing two nights running), **rotated off #3 (touches-per-dollar, proposed 6/21, carried)** and **PROPOSED #4 Event/Kinetic sponsorship yield view** for tonight. Novelty check clean: never proposed (last 7 nights = 6/18 daily lanes, 6/19 #1 pricing intel, 6/20 #2 signal feed, 6/21 #3 touches-per-dollar); codebase check confirmed sell-through service+screen exist ONLY for newsletter inventory (`newsletter_sell_through.rb` / `sell_through.html.erb`), NOT for events, and the data models are real (`sponsorship_option` = tiers, `sponsorship_selection` = sold/reserved tied to deal/event_sponsor, `event_sponsor`) → genuinely undone with real data behind it. Scoped it real-data-only: no invented per-tier capacity (no such column), so "sell-through" = sold/reserved mix + realized-vs-default $, full-members-only for $. No new build candidates from open Console cards (all open 💡 are inbox-sweep/EOD/newsletter routine-prompt hardening, not Savant product slices). Live revenue sourced from /api/v1/deals (paginated, 109 deals) + /api/v1/renewals: **booked $668,422 (42 won, Aris Machina's $15K landed today) / gap $331,578**, open pipeline 60 deals = $214,500, late-stage $197,000 (11 proposal_sent $153K + Duro contract_sent $44K), renewals 5 = $41,758. Remaining queue after #4: #5 product pulse (told Brian the queue is thin, invited fresh ideas).
- 2026-06-17 — Backlog created. Seeded Shipped from the 6/12-6/14 ledgers + brain log; verified candidates 1-5 undone against the codebase; recorded the $1M screen permanent exclusion. Replaces "re-derive from scratch" with "pull next undone."
- 2026-06-18 — Reconciled the 6/18 ledger: moved gap-closing pipeline (deal_brief API + Pipeline worklist), PWA + mobile nav, and Monaco Phase 1 (foundation + Overview) into Shipped. Added Brian's 2026-06-18 Monaco-redesign directive as Directed active work (overnight-safe, presentation-only, outranks the generic queue) and PROPOSED tonight's slice = the 3 daily operating lanes (Tasks, Approvals, Pipeline + Deals kanban). Generic candidates 1-5 (Pricing intel, Signal feed, etc.) still undone, parked behind the directed work; offered Pricing intel as the pivot option in the email.
- 2026-06-21 — Reconciled today's 6/21 build loop against origin/main. **#2 Signal feed SHIPPED** (proposed 6/20, Brian green-lit; commits aa3f21f `Crm::SignalFeed` service + b0f8619 `/signals` screen under Revenue nav, both on `origin/main`; the 6/21 ledger stalled mid-write at 07:58 ET but the commits landed) → moved to Shipped table. With #2 done, pulled the next undone Overnight-safe candidate = **#3 Touches-per-dollar / deal activity metric** and PROPOSED it for tonight. Novelty check: never proposed before (last 7 nights: 6/18 daily lanes, 6/19 #1 pricing intel, 6/20 #2 signal feed); codebase check confirmed `Activity`/`deal_activities` + `outbound_emails` data sources exist on Deal but `grep -i touch_count|touches_per_dollar|deal_activity` returns NOTHING → genuinely undone, with real data behind it. Revenue context sourced live from /api/v1/deals (paginated, 109 deals) + /api/v1/renewals: booked $653,422 / gap $346,578, open pipeline $229,500, late-stage 13 deals = $212K (11 proposal_sent $153K + Duro contract_sent $44K + Aris negotiation $15K), renewals 5 = $41,758. Remaining queue after this: #4 event sponsorship yield, #5 product pulse.
- 2026-06-20 — Reconciled run #2 + run #3 of the 6/19→20 build loop (NOW.md baton). Moved into Shipped: **#1 Pricing intelligence read view** (run #2, 0dff010+2160915, prod-verified won=41/lost=7, win 85%, median $10K) and the Brian-directed dashboard calm-down + revenue quick-view band (run #3, 5c5e7ba/e9d7fb7/216c177/4448fee). With #1 done, pulled the next undone Overnight-safe candidate = **#2 Signal feed / prioritized prospect worklist** and PROPOSED it for tonight. Novelty check: never proposed before; codebase check confirmed `AccountSignal` model + `api/v1/account_signals` + per-deal signal display all exist, but NO signal-ranked worklist screen (no `/signals` UI route) → genuinely undone. Revenue context sourced live from /api/v1/deals + /api/v1/renewals: booked $653,422 / gap $346,578, open late-stage 13 deals = $212K (11 proposal_sent $153K + Duro contract_sent $44K + 1 negotiation $15K), renewals 5 = $41,758; the 47 unpriced cold leads in open pipeline are exactly what a signal worklist would prioritize. Remaining queue after this: #3 touches-per-dollar, #4 event sponsorship yield, #5 product pulse.
- 2026-06-19 — Reconciled the 6/18 + 6/19 ledgers + origin/main. Moved into Shipped: Monaco look across all core screens + kanban drag fix (76b520b), Ask Savant slice 1 read (a4abee8 + Ollama 42ce4c3), the full multi-user access matrix (635c35e…aa39801, 8de18c5, prod-verified), Ask Savant slice 2 actions (98df743), and two cross-session wins (queue-health badge #122, Person `partner` contact_type #121). Marked the Directed active work SUBSTANTIALLY COMPLETE: Daily-operating-lanes DONE (absorbed by the foundation), Ask Savant agent SHIPPED (both slices), remaining reskin rows = optional polish. With no directed work open, pulled the top generic candidate = **#1 Pricing intelligence read view** and PROPOSED it for tonight (booked $653,422 / gap $346,578, $223.5K open across 24 priced deals incl. 10 proposals sent). Novelty check: Pricing intel was only ever "offered as a pivot option" 6/18, never the primary pick — clean to carry now. Told Brian in the email that the directed queue is clear and the overnight queue is down to a few revenue-analytics reads (signal feed, touches-per-dollar), inviting fresh ideas.
