# Build Loop — 2026-06-26 (interactive, daytime)

**Task:** Find + fix bugs in the Kerri Slack agent stack ("Slack digital worker for Savant" in kerrihq-rails + the shared Ask Savant tool layer + the Node kerri-slack-daemon), and propose material improvements to make it a real "digital worker" in Slack and on the website.

**Mode:** Interactive — Brian is awake. Can ask on a real fork. External-send rails relaxed but no real external sends without approval.

## Scope inventory
- Node daemon: `~/Projects/kerri-slack-daemon/{index.js,format.js,format.test.js}` (no git; .bak history) — shells to `claude -p`.
- Rails agent stack (`~/Projects/kerrihq-rails`), recent commits (the "ton of code"):
  - Slack digital worker: `app/services/slack/{event_processor,socket_mode_runner,socket_mode_connection,message_formatter,client,user_resolver,request_verifier,configuration}.rb`, `app/jobs/slack_event_job.rb`, `app/controllers/api/webhooks/slack_events_controller.rb`
  - Ask Savant core: `ask_savant/{agent,tool_executor(1360L),tools(552L),system_prompt}.rb`, `ollama_client.rb`, `ask_savant_controller.{rb,js}`
  - Tool clients: `microsoft_graph_client.rb`, `gmail_mailbox_client.rb`, `google_calendar_client.rb`, `reclaim_{client,mcp_client,oauth_client}.rb`, `code_shipping_client.rb`, `code_context.rb`
  - Memory: `agent_memory_store.rb`, `app/models/agent_memory.rb`, migrations

## Ledger

### Item 1 — Slack Socket Mode blocking (SHIPPED to branch+PR, not deployed)
- BRAINSTORM: 3 approaches — (a) enqueue to existing SlackEventJob [chosen], (b) spawn a thread per event, (c) cut MAX_ITERATIONS/timeout. Chose (a): reuses the SlackEventJob+Solid Queue infra that already existed for the webhook path, persists in-flight work, gives 3-way concurrency, smallest diff. Rejected (b) no persistence/back-pressure; (c) only narrows the window, doesn't fix ping starvation.
- BUG: Socket Mode read loop ran the full agent inline (handle_envelope→EventProcessor#call→multi-call Ollama loop). Blocks ping/pong → socket killed on slow requests; serializes all requests per workspace.
- FIX: default processor_factory now enqueues SlackEventJob, returns :enqueued in ms. Read loop stays responsive; Solid Queue (async, 3 threads, same worker proc) runs the agent off-loop. Injectable seam preserved.
- VERIFY: new spec (acks then enqueues) green; full non-browser suite 1951 ex / 0 failures; rubocop+brakeman clean; 20 system failures are pre-existing/environmental (unbuilt assets in worktree).
- ISOLATION: done in worktree ~/Projects/kerrihq-rails-buildloop off origin/main (9b48fab) because Codex was concurrently mutating the main working tree. Branch fix/slack-socket-async-dispatch → PR #146. NOT merged/deployed (Brian's live prod worker; awaiting his go).
- COMPOUND learning: when an agent run is the unit of work, never run it inside a transport read loop (websocket/SSE/poll); enqueue. Both Slack ingress paths (webhook + socket) must converge on the same job so the transport stays a thin acked relay.

### Smaller findings (not fixed; surfaced for decision)
- Graph reply_message force-adds default CC (brian@hfyi) even when replying to Brian, because `to:` is nil at patch time so the `direct.include?` guard can't fire. Safe direction (over-CC, Graph dedups). Send-authority-adjacent → not silently changed; flag to Brian.
- Gmail reply_message accepts suppress_default_cc but ignores it (Gmail mailbox has no default CC by design) — dead kwarg, latent trap.
- Reclaim list_tasks reports count=min(size,limit) — can't tell when results were truncated. Cosmetic.
- Calendar create_event kwargs vs normalized input: VERIFIED no bug.

### Item 2 — Coding-harness parity (the agent "gives Brian a hard time about coding")
Triggered by the live Slack thread (DM, 2026-06-26 21:35–22:52): Brian asked Kerri to execute code improvements one by one; she couldn't, and repeatedly told him she "has no tool to commit / can only ship through Copilot."

DIAGNOSIS (4 compounding harness gaps, all confirmed in code):
1. ship_code direct mode only accepted `changed_files` = WHOLE file contents → to change 3 lines of a 1361-line file she had to re-emit the entire file.
2. read_code_file had max_chars (cap 30K) but NO offset → a 50K-char file was permanently truncated; she couldn't even see/copy the whole file.
3. OllamaClient DEFAULT_MAX_TOKENS=1500 → she could never emit a 50K-char file anyway.
4. Only fallback was GitHub Copilot Coding Agent, which 409'd ("repo does not have CCA enabled"). Dead end → she wrongly claimed she couldn't write code (her own open_direct_pull_request already commits via the contents API, no Copilot).

FIX (branch fix/agent-code-editing → PR, isolated worktree off origin/main; Codex still active in main tree):
- NEW edit primitive: ship_code accepts `edits: [{path, old_string, new_string, replace_all}]`. CodeShippingClient fetches each file at base_ref, applies exact string replace server-side (literal match via block replacement; not-found and not-unique raise clear re-read hints), commits + opens a draft PR. She emits tiny hunks, not whole files — mirrors Claude Code/Codex Edit. changed_files retained for NEW files.
- read_code_file + CodeContext#read gain `offset` → page a large file fully (offset/next_offset/total_chars/truncated).
- agent loop max_tokens raised to 4096 (was 1500) for real work in one turn.
- Hosted-agent CCA 403/404/409 now raises actionable guidance ("ship yourself with edits") so she self-corrects instead of dead-ending.
- system_prompt + tool descriptions rewritten: she writes/ships code HERSELF, never needs Copilot, never says she can't; read fully→edit→ship loop spelled out.
VERIFY: rubocop+brakeman clean; new specs (offset paging; edits fetch+patch+PR; not-found; not-unique; CCA-409 guidance; edits passthrough+audit) green; full non-browser suite 0 failures (fixed 2 pre-existing strict OllamaClient.chat stubs that pinned |messages:, tools:| — legit signature change). 20 system failures pre-existing/environmental (unbuilt assets).
COMPOUND: a coding agent needs the Edit primitive (small search/replace applied server-side) + full-file paged reads + an output budget that fits a hunk, not the dependency on a hosted coding agent. Whole-file replacement is a non-starter for any real file. Adding a kwarg to a shared client method breaks specs that strict-destructure the stub block — sweep for `do |a:, b:|` stubs.

### SHIP — merged + deploying (Brian: "merge and deploy", 2026-06-26)
- CI green on both PRs (lint, scan_ruby, test, AND system-test — confirming the 20 local system failures were environmental/unbuilt-assets only).
- Merged #147 (code editing) → main 359388e, then #146 (socket async) → main 0a2f984. Both squash-merged, branches deleted. No conflict with Codex's concurrent main work.
- Render auto-deploy of 0a2f984 queued on BOTH services: web (srv-d8kvn767r5hc739fjo9g, kerrihq-rails = Ask Savant + webhook) and background worker (srv-d8kvnkcvikkc73c8cm6g, kerrihq-queue = Slack Socket Mode agent). Polling to live + prod health verify.

### DEPLOY VERIFIED LIVE (0a2f984)
- web (srv-d8kvn767r5hc739fjo9g): status=live on 0a2f984; pre-deploy (db:prepare+seed) ran; GET /up = HTTP 200.
- worker (srv-d8kvnkcvikkc73c8cm6g): status=live on 0a2f984. Logs confirm clean boot of new code: "[render-worker] started Slack Socket Mode thread", "[Slack] Socket Mode configured workspaces: T08A2U02GHJ, T09UJ7NHRK2", both workspaces "Starting Socket Mode worker", Solid Queue async Supervisor/Dispatcher/Worker started (the new off-read-loop dispatch path). No errors/disconnects.
- CI on both PRs was fully green incl. system-test (asset-built env), confirming the 20 local system failures were environmental only.
RESULT: Both fixes are LIVE in production. The Slack agent can now ship code itself via targeted edits (no Copilot dependency) and no longer blocks its socket on long runs.

### Item 3 — "I could not put together an answer" (Brian: diagnose; bug or genuine?)
ANSWER: real unflagged bug, not non-understanding. Proven: (a) Slack shows it fires on long/complex coding prompts; (b) a post-deploy prod SlackEventJob ran 46.8s and was logged as a clean success with the dead-end reply, no error; (c) direct glm-5.2 probe shows message keys [role, content, reasoning] — the model puts chain-of-thought in `reasoning` and the answer in `content`; on heavy prompts reasoning eats the whole max_tokens budget so `content` returns empty. Agent read only content → printed fallback → discarded 6000+ chars of the model's work → recorded success with no log.
FIX (PR #148, branch fix/agent-empty-reasoning-answer, agent.rb only): budget 4096→8192; on empty content, retry "answer now, no deliberation" (covers no-tools case the old tools-only synthesis missed); last-resort surface reasoning; else fallback WITH error=empty_model_response + log finish_reason/usage/reasoning len. Live-probed glm-5.2: recovery prompt returns 9k chars content at 2490 tokens. Full non-browser suite 0 failures. NOT yet merged/deployed — awaiting Brian.

### Harness rebuild toward Codex-class (Brian: make the Slack agent a reliable backup agent)
Decisions: FULL self-verify CI loop; run-to-completion (ask only if unclear). Building in shipped phases.
PHASE 1 — agentic engine (branch feat/agent-harness-core, agent.rb rewrite + system_prompt + tests; supersedes #148, which is closed):
- Loop budget: 12 iterations + 240s wall-clock deadline w/ graceful wrap-up (was hard 6).
- Robust I/O: reasoning-vs-content, empty-content recovery call ("answer now"), reasoning last-resort, empty-run flagged (error=empty_model_response) + finish_reason/usage logging. (folds #148)
- Run-to-completion: nudges the model when it announces a step instead of taking it (capped 2); system prompt now says act/finish, don't announce.
- Resilience: malformed tool-arg JSON fed back as an error (not executed with garbage); tool results capped to 8k; context compaction stubs oldest tool results past a 90k-char budget.
- Verify: 19 agent specs (6 new) green; ask_savant+slack+request+ollama 98 green; brakeman clean; full suite pending. Live-probed glm-5.2 earlier confirms recovery yields content.
PHASE 2 (next) — self-verify CI loop: read PR checks + failing Actions logs, push follow-up fixes to the same PR until green. PHASE 3 — planning/progress posting. PHASE 4 — model routing (coding->stronger Ollama model) + eval harness.

## OVERNIGHT (Brian: build + ship the whole harness end-to-end, loop for no bugs; gone to bed)
Posture: unattended build loop. Each phase = branch → tests → rubocop/rspec/brakeman → PR → CI green → merge → deploy → verify prod → next. Hard rails held (no external sends, no destructive DB, roll back on prod break). Codex still active in main tree → every phase built in an isolated worktree off origin/main.

PHASE 1 (#149) SHIPPED+LIVE: agentic engine (budget loop 12 iters/240s, run-to-completion nudges, empty-content recovery + reasoning fallback + flagged empty runs, malformed-arg feedback, context compaction). Deployed 2716652, web /up 200, worker booted clean both workspaces. Closed #148 (folded in).
PHASE 2 (#150) SHIPPED+LIVE: self-verify CI loop — check_pull_request (combined check-runs+statuses; waits bounded; fetches failing Actions job-log tails via the 302→signed-blob the client follows) + revise_pull_request (push fixes to the open PR branch). Slack deadline raised to 900s for CI cycles. GitHub API shapes validated live vs PR #149 (incl. the misleading pending-status-with-0-statuses, correctly ignored). Deployed 737be60, web /up 200, worker clean.
PHASE 3 (#151) eval battery: live end-to-end RSpec (RUN_LIVE_EVAL=1) running the real agent vs glm-5.2; skips in CI. RAN LIVE: 4/4 pass in ~10s (plain Q no-tools, revenue Q→pipeline tools, Duro→find_deals, capability no-dead-end). PR open, CI pending.
PHASE 4 (#152) opt-in coding-model routing: after a code tool, route rest of task to ASK_SAVANT_CODE_MODEL (qwen3-coder-next) if set; default OFF (no prod change). Validated live: qwen coder models speak OpenAI tool-calls, no reasoning field, and correctly continue a glm tool exchange (handoff works). PR open, CI pending.

Remaining tonight: merge #151+#152 on green, deploy, verify, write morning Console report. Routing stays OFF in prod (Brian flips ASK_SAVANT_CODE_MODEL when ready).
LEARNING: glm-5.2 puts answers in `content`, thinking in `reasoning`; budget starvation → empty content (the dead-end bug). Coder models (qwen3-coder*) have no reasoning field. A coding agent's defining feature is verify-via-CI (read checks + logs, push fixes), not just open-a-PR. Cross-model mid-conversation handoff works on Ollama as long as message format is OpenAI-standard.

## OVERNIGHT COMPLETE (2026-06-27 ~00:29 ET)
All 4 phases SHIPPED + MERGED + DEPLOYED + VERIFIED LIVE on 43b53a2 (web /up 200; worker booted clean, both Slack workspaces; only "errors" = expected deploy-transition socket reconnect). PRs #149 #150 #151 #152 merged. #148 closed (folded into #149). Morning report filed to Console (action_needed). Coding-model routing wired but OFF by default (Brian flips ASK_SAVANT_CODE_MODEL=qwen3-coder-next on web+worker to enable). No external sends, no destructive DB, no prod breakage. Codex's concurrent main-tree work never collided (all phases built in isolated worktrees, now cleaned up).
