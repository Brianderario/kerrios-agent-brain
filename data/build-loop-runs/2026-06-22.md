# Build Loop — 2026-06-22 (overnight)

Task: Event sponsorship-yield reporting on Savant (kerrihq-rails).
Branch: `bl/event-sponsorship-yield` off origin/main (9a7bf59).
Worktree: ~/Projects/kerrihq-rails-bl. Isolated DB: kerrihq_rails_bl (DATABASE_URL).
Hard stop 07:00 ET, wrap 06:30 ET. Start 23:16 ET.

## Setup
- 23:16 fetched origin, branched off origin/main 9a7bf59. Worktree reused (was bl/signal-feed).
- Postgres@17 running. Assets/builds present in worktree. kerrihq_rails_bl DB exists.

## Schema facts established (real, from db/schema.rb + models)
- sponsorship_options (tier): default_value_cents, name, event_id, organization_id, position. `default_value` = cents/100.
- sponsorship_selections: belongs_to sponsorship_option (req), deal (opt), event_sponsor (opt); value_cents; must have deal OR event_sponsor.
- deal.stage enum: lead0 qualified1 proposal_sent2 closed_won4 closed_lost5 contract_sent6 negotiation7. CLOSED=[closed_won,closed_lost]. open? = not closed.
- event_sponsor.pipeline_status enum: pending0 outreach_sent1 lead_qualified2 proposal_sent3 negotiation4 closed_won5 closed_lost6 contract_sent7. OPEN_STAGES, CLOSED_STAGES.
- NO "confirmed" column/state on event_sponsor → "confirmed" = closed_won? pipeline_status; "unconfirmed" = open pipeline. (grep confirmed = none.)
- events.restricted boolean default false. DealPolicy::Scope + EventPolicy::Scope exclude restricted events a user lacks EventAccess for.
- can_see_pipeline?(org) = super_admin? || full_member_of? (User#can_see_pipeline?, user.rb:85).

## Items
### ITEM 1 — BRAINSTORM/PLAN (23:30 ET)
Approaches considered: (a) scope via DealPolicy::Scope on the selections' deals only — rejected: misses event_sponsor-based selections and the tiers/options themselves; restricted-event concern is event-level. (b) scope the EVENT via policy_scope(Event)=EventPolicy::Scope (the event analog of DealPolicy::Scope; both exclude restricted events a user lacks EventAccess for) — CHOSEN: all of an event's selections belong to that one event, so gating the event fully carries restricted access through, and the service stays event-centric like its caller. (c) no scope, gate only in controller — rejected: contract wants the exclusion provable in the service spec.
Plan: `Events::SponsorshipYield.new(event:, scope: Event.all)`. If event not in scope → empty result (no tiers, zero totals). Else preload options→selections→[deal,event_sponsor], classify each selection: SOLD if deal closed_won OR event_sponsor pipeline closed_won; RESERVED if (not sold) AND (deal.open? OR event_sponsor.pipeline_open?); else EXCLUDED (closed_lost/lost = neither, honest). Per tier: sold_count/cents, reserved_count/cents, default_value_cents (list), total. Event totals: total_sold_cents, total_reserved_cents, tier_count. "Realized vs default" = expose realized (sold $) + per-unit list ($) as raw numbers; NO invented ratio/capacity. Spec: sold/reserved/lost derivation (deal + event_sponsor paths) + restricted-event scope exclusion. Done = service correct on real-shaped data, rspec/rubocop/brakeman green.

### ITEM 1 — REVIEW + SHIP (00:05 ET)
- 12/12 new specs green. rubocop clean (2 files). brakeman 0 warnings.
- Full suite: 1789 examples, 1 failure, 1 pending. The 1 failure is PRE-EXISTING + unrelated: `spec/requests/api/newsletter_inventory_imports_spec.rb:17` — `placement.sponsor_commitment.asset_due_on` expected Sat 13 Jun 2026, got nil. Reproduces in isolation; my change is only 2 new untracked dirs (app/services/events, spec/services/events) loaded by nothing else. Date-sensitive newsletter-import test, separate domain. NOT mine, NOT touched (additive-only, out of scope). Flag in morning report.
- ITEM 1 has no prod-deploy DONE step (service-only); ships with ITEM 2's deploy.

### ITEM 2 — WORK/REVIEW/SHIP (00:55 ET)
- Controller: EventsController#show builds @sponsorship_yield = Events::SponsorshipYield.new(event:, scope: policy_scope(Event)) + @can_see_pipeline = current_user.can_see_pipeline?(@organization).
- Partial app/views/events/_sponsorship_yield.html.erb (strict locals; short leading <%# locals %> to dodge the ERB-comment landmine). Per-tier table (sold count+$, reserved count+$, realized vs list, tier total) + event total tfoot. Counts always shown; ALL $ gated behind can_see_dollars. Reserved figures use safelisted `warning` token (amber is NOT safelisted → switched). Section rendered in show.html.erb after Sponsor Acquisition, only when tier_count>0.
- Request spec spec/requests/events_sponsorship_yield_spec.rb: full member sees "$1,234" (reserved, section-unique since open deals aren't booked revenue); event_guest sees the section + "Figures shown to full members" but NOT "$1,234". Both green.
- rubocop clean (ruby files), brakeman 0. Full suite 1791 ex, 1 failure (same pre-existing newsletter one), 1 pending.
- Committed 894aff3 + 2eb6804.

### DEPLOY BLOCKED (01:05 ET) — classifier, twice
- `git push origin HEAD:main` (the build-loop's normal deploy trigger) DENIED by auto-mode classifier as a default-branch push. Did NOT bypass.
- Fallback (review-respecting): pushed branch `bl/event-sponsorship-yield` to origin + opened **PR #131** (https://github.com/kerrihq/kerrihq-rails/pull/131). Merging it → Render auto-deploys.
- Attempted the read-only real-data verify (prod `rails runner`, SELECT/print only, like the PricingIntelligence item) — also DENIED by the classifier (Production Reads). Did NOT bypass.
- NET: ITEM 2 code complete + fully verified LOCALLY; the deploy + on-prod render/verify await Brian merging PR #131 (one click). This is the only unmet DONE step, blocked by a permission rule with no human awake to approve.

- ITEM 1: Events::SponsorshipYield read-only aggregate service. STATUS: SHIPPED to branch/PR #131; awaiting merge→deploy. Code + specs DONE + green.
- ITEM 2: Sponsorship yield section on event show page, gated can_see_pipeline?. STATUS: code DONE + verified locally (specs green, gate proven); PARKED on deploy+prod-verify (PR #131 merge needed; classifier blocked the auto-deploy path overnight).
