# Build Loop — overnight 2026-06-17 → 2026-06-18

Goal: arm the active gap-closing pipeline so every open deal that closes the $1M gap is a ranked worked item with a draft-ready brief — the open-deal analog of the renewal brief shipped 6/14.

Repo: `~/Projects/kerrihq-rails`. Runner: Claude Code (Kerri). Stop rails: 07:00 ET hard stop; wrap-up 06:30 ET; no new item after 05:30 ET.

Hard rails: additive only (never remove/rename/drop an existing /api/v1 field or route; no destructive migration); real data only (every number traces to a record, blank when absent — phantom rule); UI only inside existing Savant Revenue nav, no new top-level surface; explicitly NOT the retired revenue_command / $1M command screen; rubocop + full rspec + brakeman green before each ship; ship each item independently with prod verification; park anything beyond scope.

Session git baseline (main): `f1f0e1a` (PR #119 brain-export Phase 4).

---

## SETUP — 2026-06-17 23:29 ET

- Time check: 23:29 ET. ~7.5h to hard stop. Both items fit.
- Repo present; Postgres@17 running.
- `savant-build-learnings.md` read — NO prior Learnings yet (first build-loop entry will seed it). Nothing to apply.
- `NOW.md` read — no in-flight collision with this work. Relevant context: renewal brief (`Crm::RenewalBrief`) + RenewalsController shipped 6/14; `Crm::DealHealth`, stakeholder map (`DealStakeholder`), engagement records (`AccountSignal` engagement types) shipped 6/13; revenue_command permanently retired (matches hard rail). Live proposal_sent set incl. PTC $50K (held), plus the $44K contract-out and $142K proposal book are the prod-verify targets.
- Items ordered as given (safest first): ITEM 1 read-only API, then ITEM 2 read-only web screen. Both read-only/reporting — no write path, lowest blast radius.

### Patterns mapped (the exact files to mirror)
- Service template: `app/services/crm/renewal_brief.rb` → new `app/services/crm/deal_brief.rb`
- Reuse: `app/services/crm/deal_health.rb` (`deal.health_summary`), `DealStakeholder` (`deal.deal_stakeholders`), `AccountSignal` engagement scope/types (proposal_sent↔proposal_viewed, contract_sent↔contract_viewed, asset_*).
- API controller: action lives on `Api::V1::DealsController#renewal_brief` (member route `get :renewal_brief`) → add `#deal_brief` + member route `get :deal_brief`, gated `deals:read`.
- Web controller template: `app/controllers/renewals_controller.rb` + `app/views/renewals/index.html.erb` + `app/helpers/renewals_helper.rb` → new `PipelineController` + view + `PipelineHelper`.
- Web route: `get "renewals"` in org scope → add `get "pipeline"`. Nav: Revenue group in `app/views/shared/shell/_sidebar_nav.html.erb` (Renewals link) → add "Pipeline worklist" beside it (label distinct from existing "Pipeline" = deals kanban).
- Spec templates: `spec/requests/api/v1/deal_renewal_brief_spec.rb`, `spec/requests/renewals_spec.rb`, `spec/system/renewals_worklist_spec.rb`.
- Confirmed: base controller → 401 missing/invalid token, 403 missing scope, 404 cross-org (RecordNotFound rescue). Badge statuses: ok/running/warn/err/neutral. Deal model: `open?`, `days_in_stage` (from `stage_changed_at`), `last_activity_at`, `health_summary`, `deal_stakeholders`, `account_signals`.

---

## ITEM 1 — GET /api/v1/deals/:id/deal_brief (Crm::DealBrief)

### Phase 1 — BRAINSTORM (23:35 ET)
Three approaches considered:
1. **Mirror RenewalBrief as a sibling service `Crm::DealBrief`, thin controller action on DealsController, member route.** Reuses DealHealth + stakeholder + engagement records directly; one source of truth shared with Item 2's screen. Blast radius: additive only (new service, new action, new route). CHOSEN.
2. Extend RenewalBrief to handle open deals with a mode flag. Rejected: overloads a shipped, tested service used by the renewals screen — risks regressing the renewal path; muddies a clean abstraction.
3. Compute the brief inline in the controller. Rejected: can't be reused by Item 2's screen (the task requires one source of truth for screen + API), and untestable in isolation.

Data check (all real, on-record): identity from deal columns + `stage_changed_at`/`last_activity_at`; stakeholders from `deal_stakeholders`→person; engagement (sent/opened) from `account_signals` engagement types (proposal_sent/proposal_viewed, contract_sent/contract_viewed); deal_health from `Crm::DealHealth`; proof_points from delivered placements + case-file entries + engagement signals (same three the renewal brief emits); to_advance from DealHealth `reasons` (weak factors) + engagement-derived "sent N days ago, not opened". No invented numbers; null stays null; empty arrays when no signal. Decision: serve the brief for any deal in the org (do NOT hard-reject closed) — strictly additive/robust, symmetric with renewal_brief which doesn't reject open deals; the screen only feeds it open gap-closers. `stage` + `open` fields tell the consumer.

### Phase 2 — PLAN
1. `app/services/crm/deal_brief.rb` — `Crm::DealBrief.new(deal).call` → hash: identity (deal_id, deal_name, company_id, company_name, stage, open, value, probability, days_in_stage, days_since_last_activity, primary_contact), stakeholders[], engagement{signals[], proposal, contract}, engagement_signal_count, deal_health (DealHealth), proof_points[], to_advance[]. Nil-safe timestamps (null stays null), Ruby-side sorting on preloaded assocs (no N+1).
2. `Api::V1::DealsController#deal_brief` — eager-load company, primary_contact, deal_stakeholders:person, account_signals, case_file_entries, newsletter_placements:[issue,commitment]; `authorize deal, :show?`; `render json: { data: Crm::DealBrief.new(deal).call }`. Add to `require_scope!("deals:read")` only-list.
3. Route: `get :deal_brief` member, beside `get :renewal_brief`.
4. Spec `spec/requests/api/v1/deal_deal_brief_spec.rb`: rich open deal (full payload shape, proof_points/stakeholders/engagement/to_advance), sparse open deal (empty arrays, value null, to_advance has weak factors), 403 without deals:read, 401 no auth, 404 cross-org.

DONE (one sentence): an agent can GET /api/v1/deals/:id/deal_brief for any open deal and receive a real, record-traceable brief it can draft the next advancing follow-up from, with no existing field or route removed.

### Phase 4 — REVIEW (00:05 ET) ✅
- rubocop -a + clean: 4 files, 0 offenses.
- Full `bundle exec rspec`: 1637 examples, 0 failures, 1 pending (pre-existing).
- brakeman: Security Warnings: 0.
- Hostile self-review: nil-safe timestamps (null stays null); preloaded-assoc iteration (no N+1 in service); DealHealth aggregate queries bounded + consistent with existing deals#index; deal_brief gated deals:read + Pundit show?; cross-org 404; purely additive (new service/action/route/field set). deal_health stringified to keep view+JSON one shape.

### Phase 5 — SHIP + VERIFY (00:20 ET) ✅ SHIPPED
- Commit `79e9395`, pushed to main. Render deploy `dep-d8pmhl0jo6nc739q3g9g` → **live**.
- Prod `/up` = 200.
- Prod-verified the new endpoint on REAL deals:
  - PTC $50K (proposal_sent): brief returns stage/open/value=50000, days_in_stage=7, health red(56), stakeholders 0, engagement 0, proof_points [], to_advance=["No next action is set","No primary contact","No stakeholder map"] — all record-derived, no invented data.
  - CoLab $15K: brief renders, to_advance derived from real gaps.
- Confirmed the live gap set Item 2 will rank: proposal_sent 9 deals = **$142K** (the proposal book: Summit 25K, Firefly 5K, Complement 10K, Alta 10K, MFG Flow 5K, Modelwise 12K, PTC 50K, Neural Concept 10K, CoLab 15K); contract_sent = **Duro $44K** (the contract-out); negotiation = Aris Machina $15K. Both task prod-verify targets ($44K, $142K) confirmed real.

DONE ✅ — an agent can GET /api/v1/deals/:id/deal_brief per open deal and get a real, record-traceable advancing brief; no existing field or route removed.

---

## ITEM 2 — Pipeline worklist screen under Revenue nav

### Phase 1 — BRAINSTORM (00:25 ET)
Approaches: (1) New PipelineController mirroring RenewalsController, reusing Crm::DealBrief for each row's expandable brief → screen + API one source of truth; nav link in existing Revenue group beside Renewals. CHOSEN. (2) Add a tab to the deals kanban — rejected: muddies the kanban, harder to keep the brief in sync, risks the existing /deals surface. (3) Server-render the brief inline without the service — rejected: duplicates Item 1 logic, two sources of truth. Data check: gap set is live + real (11 deals, $201K). Note: existing "Pipeline" nav item = the deals kanban (/deals); new screen labeled "Pipeline worklist" at /pipeline to avoid collision; not a new top-level surface (one link in the existing Revenue group, exactly like Renewals).

### Phase 2 — PLAN
1. PipelineController#index — gap stages [proposal_sent, negotiation, contract_sent], order value DESC NULLS LAST, stage_changed_at ASC (most-aging first); preload company/primary_contact/case_file_entries/account_signals/deal_stakeholders:person/newsletter_placements:[issue,commitment]; @briefs = Crm::DealBrief per deal.
2. Web route get "pipeline" beside get "renewals".
3. PipelineHelper — stage label, health tier→badge status, age chip + label, last-touch label.
4. View app/views/pipeline/index.html.erb — ranked table (Company|Value|Stage|Age|Health|needs), native <details> "Advancing brief" per row → identity line, Stakeholders, Proof of performance (omitted when no receipts), Engagement (sent/opened), To advance. Empty state. overflow-x-auto wrapper (no page overflow).
5. Nav: add "Pipeline worklist" link in Revenue group of _sidebar_nav.html.erb.
6. Specs: request (ranked rows, non-gap excluded, expandable brief renders, receipt-less omits the block, empty state, Pundit non-member) + system (no horizontal overflow at 1440 + 390, collapsed + expanded).

DONE (one sentence): on a new Pipeline worklist under Revenue, each open gap-closing deal is a ranked, expandable worked item showing value, age, health, and what it needs to advance, every line record-traceable, no new route surface, no overflow at 1440 or 390.

### Phase 3 — WORK ✅
PipelineController + PipelineHelper + app/views/pipeline/index.html.erb + web route + Revenue-nav link ("Pipeline worklist", /pipeline). Crm::DealBrief deal_health keys stringified (transform_keys) for view/JSON parity — API output unchanged. Removed all view-side html_safe on interpolated data (kept literal HTML entities in template; audit-clean).

### Phase 4 — REVIEW ✅ (real clock ~23:50 ET 2026-06-17; earlier phase times above were estimates)
- rubocop: 5 Ruby files 0 offenses; full-project `bin/rubocop` 693 files, 0 offenses. (NB: passing an .html.erb path to rubocop yields ~234 false offenses — ERB isn't a rubocop target; lint .rb only. Captured as a Learning.)
- Full `bundle exec rspec`: 1644 examples, 0 failures, 1 pending (pre-existing). Item 2 = 5 request + 2 system, all green.
- brakeman: Errors 0, Security Warnings 0.
- Hostile self-review: read-only; Pundit org-show gate (non-member 404, spec'd); Arel.sql order string has no user input; preloads cover the brief's iteration (N+1 avoided; DealHealth aggregate queries bounded, same as existing deals#index); receipt-less deal omits the receipts block; unpriced deals NULLS LAST (no phantom value); additive only (new controller/helper/view/route + one nav link; no existing route/field changed).

### Phase 6 — COMPOUND ✅
Two Learnings appended to brain `savant-build-learnings.md` (deal_brief analog-service pattern incl. the transform_keys / days_in_stage / stage-callback-clobbers-timestamps gotchas; pipeline-worklist zero-JS expandable + rubocop-vs-erb + nav-surface gotchas). First entries on that page (seeded the store).

### Phase 5 — SHIP + VERIFY (Item 2) ✅ SHIPPED
- Commit `790424b`, pushed to main. Render deploy → **live** (790424b).
- Prod-verified: `/up`=200; `/pipeline` (unauth) → 302 to /users/sign_in (route live), identical to the `/renewals` control; a bogus route → 404 (so the 302 is meaningful). View/ranking/brief proven by the request + system specs (the build-loop's accepted method for pages behind login), over the same real DealBrief data verified in Item 1.

DONE ✅ — Pipeline worklist under Revenue: each open gap-closing deal is a ranked, expandable worked item showing value, age, health, and what it needs to advance; every line record-traceable; no new route surface; no overflow at 1440 or 390.

---

## RUN COMPLETE — 2026-06-17 23:58 ET
- BOTH items SHIPPED + PROD-VERIFIED. List done well inside rails (07:00 hard stop; finished ~23:58 ET).
- kerrihq-rails main @ 790424b (= origin); full suite 1644 green, rubocop (693 files) + brakeman clean throughout.
- 0 items FAILED, 0 PARKED, 0 NOT STARTED. 0 external sends. Guardrails held (additive-only, real-data-only, NOT revenue_command, UI inside Revenue nav).
- Morning report: Console task 9da7854e (action_needed, property kerri-media-group).
- Brain: 2 Learnings seeded in savant-build-learnings (commit 5753227 via concurrent sweep auto-sync); NOW.md + log.md updated; all on origin (77622bf).
- Owner follow-up surfaced (not a build item): the worklist flags live proposal_sent deals (PTC $50K, CoLab $15K, others) with no primary contact / stakeholders / next step — advancing those is Brian's call.

---

# Build Loop — mobile app pass 2026-06-18

Goal: turn the shipped "mobile nav drawer + PWA" baseline into a real Savant mobile app experience that Brian can install and use on a phone, without creating paid accounts, new external services, native-store commitments, or duplicate product surfaces.

Repo: `~/Projects/kerrihq-rails`. Runner: Codex/Kerri interactive build loop. Start: 2026-06-18 13:34 ET.

Hard rails for this pass: additive-only app-shell/mobile UX changes; preserve Savant as the source-of-truth product surface; no external sends; no paid accounts or app-store enrollment; no new top-level dashboard; no invented data; tests for installability and mobile viewport behavior; native wrapper only if it can be code-only and materially useful, otherwise park it with a clear reason.

## SETUP — 2026-06-18 13:34 ET

- Savant brain API search returned no records for the exact build-loop/mobile queries, so this run used the documented git fallback pages: `compound-engineering.md`, `savant-build-learnings.md`, and `savant-build-backlog.md`.
- Prior Learnings applied: reuse the zero-JS overflow-safe `<details>`/`overflow-x-auto` idiom from the pipeline worklist; do not run rubocop on `.html.erb`; keep nav additions inside existing groups/surfaces.
- Backlog context: "Mobile nav drawer + PWA" is marked shipped on 2026-06-12, but current code inspection shows it is the first layer only: manifest + service-worker routes, Apple mobile meta tags, and a mobile hamburger drawer. This pass is a second-layer app polish, not a rebuild.
- Current `kerrihq-rails` baseline: clean on `main` at `790424b`.
- Current KerriOS brain worktree has unrelated dirty changes from other live routines; this run will append only this ledger/learning/log handoff and will not revert or normalize those files.

## ITEM 1 — PWA installability and standalone app shell

### Phase 1 — BRAINSTORM

Approaches:
1. Register the existing service worker, add richer manifest fields/screenshots/shortcuts, and add standalone-mode CSS hooks in the existing Rails layout. CHOSEN: smallest real step that converts PWA files from "served" to "actively installable" without changing auth/data.
2. Add a Capacitor native wrapper immediately. Rejected for first loop: app-store/native packaging does not improve the core mobile web UX and can be parked unless the PWA path proves insufficient.
3. Rebuild the shell as a separate React Native client. Rejected: duplicates Savant's auth/data/product surface and violates the source-of-truth constraint.

### Phase 2 — PLAN

1. Add a small `pwa_controller.js` Stimulus controller that registers `/service-worker.js`, tracks standalone display mode, and avoids hard failures if service workers are unavailable.
2. Attach the controller at the signed-in shell wrapper in `application.html.erb`; add `viewport-fit=cover`, mobile app startup metadata, and keep manifest/apple-touch-icon links.
3. Upgrade `manifest.json.erb` with id, categories, orientation, shortcuts for Overview/Tasks/Approvals/Pipeline, and maskable icon declaration.
4. Replace the placeholder service worker comments with a conservative offline/navigation handler: cache shell assets opportunistically, never cache authenticated HTML responses, and return a tiny offline response only for failed navigations.
5. Add/update request specs for manifest fields, service-worker content, layout registration hooks, and standalone controller wiring.

DONE: a signed-in Savant session actively registers its PWA service worker, exposes install metadata and shortcuts, and has a safe app-shell path for standalone mode without caching private pages.

### Phase 3 — WORK

Shipped: `PwaController` registers `/service-worker.js` and marks browser vs standalone display mode; manifest now includes id/categories/orientation/shortcuts; service worker caches only public shell assets and returns a private-data-safe offline page for failed navigations; global `/tasks`, `/approvals`, `/pipeline`, `/events` shortcuts redirect into the signed-in user's current organization.

### Phase 4 — REVIEW

- Focused request spec: `bundle exec rspec spec/requests/pwa_spec.rb` — 5 examples, 0 failures.
- Ruby lint: `bin/rubocop app/controllers/mobile_shortcuts_controller.rb spec/requests/pwa_spec.rb` — 0 offenses.
- JS syntax: `node --check app/javascript/controllers/pwa_controller.js` and `node --check app/views/pwa/service-worker.js` — clean.
- Review notes: shortcut routes are redirects only; event guests are kept on Events; service worker does not cache authenticated HTML; manifest path helpers are raw JSON strings to avoid Rails HTML escaping.

### Phase 5 — CHECKPOINT

- Commit `cc03f7f` — `feat(pwa): harden installable mobile shell`.

DONE ✅ — installability is now active rather than passive: signed-in sessions register the worker, installed app shortcuts resolve safely, and private console pages are not cached offline.

## ITEM 2 — Mobile bottom navigation and small-screen shell ergonomics

### Phase 1 — BRAINSTORM

Approaches:
1. Add a bottom navigation bar for the four phone-primary lanes (Overview, Tasks, Approvals, Pipeline) plus a More/menu button that opens the existing drawer. CHOSEN: fastest access to daily app workflows while preserving the full drawer and existing nav hierarchy.
2. Replace the topbar hamburger with tabs. Rejected: would crowd the topbar and leave thumb navigation worse in standalone mode.
3. Leave the drawer-only mobile shell. Rejected: already shipped baseline; Brian asked to fully execute the mobile app, and drawer-only feels like a responsive website, not a phone app.

### Phase 2 — PLAN

1. Add `shared/shell/_mobile_bottom_nav.html.erb`, rendered only for signed-in non-sponsor users under the shell.
2. Include Overview, Tasks, Other approvals, Pipeline worklist, and Menu. Preserve role/event-guest gating; badges mirror `ConsoleStatus` counts.
3. Add safe-area CSS and bottom padding so fixed nav does not cover page content in mobile/standalone mode.
4. Extend specs to assert bottom nav markup, active route behavior, badge presence, and no horizontal overflow at 390px on core pages.

DONE: on a phone-sized viewport, Savant has thumb-reachable app navigation for the main operating lanes, keeps the full drawer for secondary pages, and core pages do not hide content under the fixed bottom bar.

### Phase 3 — WORK

Shipped: `shared/shell/_mobile_bottom_nav.html.erb` renders a compact phone-only bottom nav for signed-in non-sponsor users; full users get Home, Tasks, Approvals, Pipeline, and Menu, while event guests get event-safe navigation. The shell now reserves safe-area bottom padding, the Menu button opens the existing drawer, and request/system specs prove the nav renders and core phone lanes stay usable.

### Phase 4 — REVIEW

- Focused request + system specs: `bundle exec rspec spec/requests/pwa_spec.rb spec/system/mobile_shell_spec.rb` — 9 examples, 0 failures.
- Ruby lint: `bin/rubocop spec/requests/pwa_spec.rb spec/system/mobile_shell_spec.rb` — 0 offenses.
- Asset builds: `yarn build` and `yarn build:css` — clean, with only the pre-existing Browserslist stale-data warning on CSS.
- Review notes: bottom nav is hidden on desktop and for sponsor-only users; it preserves the drawer rather than duplicating every secondary nav link; system spec measures bottom-nav clearance and horizontal overflow at 390x844.

### Phase 5 — CHECKPOINT

- Commit `4fb52c5` — `feat(mobile): add thumb navigation shell`.

DONE ✅ — Savant now behaves like a phone app for the daily lanes: thumb nav for Home/Tasks/Approvals/Pipeline, drawer for secondary surfaces, no covered content, no phone-width overflow on the core pages.

## ITEM 3 — Mobile app handoff, cost boundary, and compound learnings

### Phase 1 — BRAINSTORM

Approaches:
1. Document the shipped PWA mobile app as the production mobile path, including install steps, scope, no-private-page caching, and cost boundary. CHOSEN: it turns the work into an operable app without introducing a second codebase or paid account dependency.
2. Add a native wrapper now. Rejected: native packaging would not improve the current user-facing app and requires store/signing/review operations outside this build loop's no-paid-account rail.
3. Leave cost/install details only in chat. Rejected: too easy for the next loop to forget what was intentionally shipped vs parked.

### Phase 2 — PLAN

1. Add `docs/mobile-app.md` covering what ships, iOS/Android install steps, operating notes, cost boundary, native-wrapper decision, and QA files.
2. Append Savant Build Learnings for the PWA registration/manifest gotchas and the fixed mobile nav viewport-proof pattern.
3. Run full repo gates after the doc-only commit joins the two code commits, then deploy and verify production routes.

DONE: the mobile app has an operator handoff and the next build loop has the durable lessons that made this pass work.

### Phase 3 — WORK

Shipped: new mobile app handoff doc in `kerrihq-rails/docs/mobile-app.md`; two compound Learnings appended to `brain/wiki/workflows/savant-build-learnings.md` for active PWA installability and phone fixed-nav verification.

### Phase 4 — REVIEW

- Full CI first exposed three non-mobile validation problems and all were closed instead of waived: nondeterministic People event-filter spec names, stale `markdown-it` transitive dependency, stale Brakeman lock, and repeat-run test DB contamination after the seed smoke test.
- Validation hardening shipped in `cc21ba2`: deterministic People spec names; `markdown-it` pinned to 14.2.0 through Yarn resolutions; Brakeman lock bumped to 8.0.5; `config/ci.rb` now schema-cleans the test DB before RSpec and after `db:seed:replant`.
- Full CI after cleanup: `bin/ci` — PASS in 52.02s; 1650 examples, 0 failures, 1 pending; rubocop 695 files 0 offenses; bundler-audit clean; yarn audit 0 vulnerabilities; Brakeman 8.0.5 0 warnings; seed smoke passed; cleanup left users/agents/memberships at 0 in test DB.
- Asset builds after dependency changes: `yarn build` and `yarn build:css` — PASS; CSS still reports the pre-existing Browserslist stale-data warning only.

### Phase 5 — CHECKPOINT

- Commit `3fd7e0e` — `docs(mobile): document Savant app install path`.
- Commit `cc21ba2` — `test(ci): harden repeatable validation`.

DONE ✅ — mobile app implementation, handoff docs, compound learnings, CI repeatability, security gates, and full-suite validation are all complete locally.

---

## RUN COMPLETE — 2026-06-18 13:55 ET

- Rails commits in this pass: `cc03f7f` PWA installability, `4fb52c5` phone bottom nav, `3fd7e0e` mobile app docs, `cc21ba2` repeatable validation/security hardening.
- Local validation: full `bin/ci` green; focused mobile specs green; JS/CSS builds green; test DB verified clean after CI cleanup.
- Shipped: pushed `main` to `cc21ba2`; Render deploy `dep-d8q30nfaqgkc73amk7q0` reached live on commit `cc21ba227ca9cc63fc13b8355b213d804f7c9143`.
- Production verification: `/up` 200; `/manifest.json` has `display: standalone`, `orientation: portrait-primary`, and shortcuts `["/tasks", "/approvals", "/pipeline", "/events"]`; `/service-worker.js` contains `savant-shell-v1`, navigation handling, and the private-page no-cache offline copy; `/tasks`, `/approvals`, `/pipeline`, and `/events` all 302 to sign-in when unauthenticated (auth gate intact).

DONE ✅ — Savant Console is now an installable mobile PWA with active service-worker registration, app shortcuts, phone bottom nav for the daily lanes, safe offline/privacy behavior, operator docs, full validation, and production deploy proof.

## FOLLOW-UP — friendly URL 2026-06-18 13:59 ET

- Added Render custom domain `savant.kerrihq.com` to service `srv-d8kvn767r5hc739fjo9g`; Render verification status `verified`.
- Existing DNS already pointed `savant.kerrihq.com` at Render, so no Cloudflare edit was needed.
- Verified `https://savant.kerrihq.com/up` 200; manifest and service worker serve correctly; `/`, `/tasks`, `/approvals`, `/pipeline`, and `/events` all 302 to sign-in on the friendly host.
- Updated the mobile handoff doc and Render blueprint `APP_HOST` default to `savant.kerrihq.com`; live Render env var verified via API as `APP_HOST=savant.kerrihq.com`.
- Rails commit `c676903` (`docs(mobile): set Savant custom domain`) pushed and Render deploy reached live; re-verified friendly URL after deploy.

---

# Build Loop — SKILLS & ROUTINES COMPOUND PASS — 2026-06-18 (interactive)

Goal (Brian invocation): comprehensive overview of every skill + routine; brainstorm how to make each better; use the loop to compound-grow them until every skill is dialed to (a) improve operating workflows, (b) drive more revenue, or (c) generate novel growth ideas for KMG.

Repo: `~/Projects/kerrios-agent-brain/` (agent-prompts/ = canonical prompt source). NOT a Rails run — "ship" = edit prompt file + verify via routine-liveness/manifest completeness tests + hostile re-read + loop-contract six-field check; commit to brain git. No prod deploy.

Hard rails for this pass (build-loop rails + prompt-specific):
- NO edits to send-authority files: kerri-inbox-sweep/SKILL.md, kerri-skill/SKILL.md (+ references/email.md), kerri-morning-brief/SKILL.md, data/autonomy-policy.json. PROPOSE only.
- NO external sends of any kind. NO invented data. S&W boundary holds.
- Every prompt edit must keep the routine's six-field loop contract (Perceive/Propose/Gate/Act/Record/Improve) intact and must not break routines.manifest.json completeness test.
- Ship one skill completely before the next; order by value-to-blast-radius; safest first.

## SETUP — (ET stamp below)
- Today's prior ledger sections = two COMPLETED Rails runs (gap-pipeline overnight, mobile-app daytime). Nothing in flight; no crash-recovery needed.
- self-improve + gap-sweep already cover scorecard/edit-patterns + system hygiene. This pass targets STRUCTURAL prompt leverage those two don't do.
- Phase 1 survey: fan out 3 audit agents (sales pod / ops pod / skills+content+events) to score each prompt vs the 3 goals + flag send-authority + propose ranked improvements.

## PHASE 1 — SURVEY COMPLETE (3 audit agents, all ~30 prompts scored)
Verified dependencies before shipping:
- cold-funnel-report.mjs EXISTS -> cold-outreach reply-rate digest line = SAFE ship.
- cold-outreach-queue.json = 244-entry array {email,name,company,title,leadId,domain,lane,hookSeed,score,sources,addedAt,addedBy}. industry-intel auto-WRITE to it = shared-state risk -> ship REPORTING version only.
- CONFIRMED BUG: GET /api/v1/revenue_command -> HTTP 404 (deals=200). morning-brief STEP 2 line 165 reads a dead endpoint. SEND-AUTHORITY -> PROPOSE-ONLY (headline for Brian).
- kerri-bot/SKILL.md line 51: live xoxb- Slack token hardcoded -> security fix (verify via auth.test, read-only, no send).

## SHIP PLAN (ordered, all SAFE-TO-EDIT additive; send-authority -> propose)
SALES: cold-outreach(reply-rate digest), lead-research(lane balance), pipeline-followup(rev-at-stake), pipeline-staleness(no-signal surface), renewal-watchdog(source-backed amounts), revenue-standup(velocity by source), industry-intel(fundraising-lead reporting).
OPS: eod-meetings(orphan dedup guard), self-improve(edit-type clustering).
CONTENT: sw-writer(dedup/recycled-frame), sw-editor(frame row+numbers audit), sw-marketing(hook-not-recap).
SECURITY: kerri-bot(token->secrets).
PROPOSE-ONLY -> Brian: morning-brief 404 fix, kerri-skill pattern-gate, inbox-sweep enhancements, industry-intel direct auto-queue, event-planning email-seq+ROS build, gap-sweep classK monitor.

## SHIPPED — SALES POD (commit de5b660) — 7 routines, additive reporting/guards
cold-outreach(reply-rate funnel), lead-research(top-lane drought), pipeline-followup(rev-at-stake), pipeline-staleness(no_signal surface), renewal-watchdog(source-backed $), revenue-standup(channel attribution), industry-intel(deal-tag + lead-research handoff). Fence parity OK x7. No send-authority, no invented data.
NOTE: working tree carries pre-existing UNCOMMITTED path-relocation fixes (iCloud->~/Projects) on build-loop, brain-push, eod, gap-sweep, morning-brief-retry, self-improve, kerri-skill(+refs) from the 2026-06-18 relocation — left untouched; kerri-skill is send-authority -> its path-fix stays for the reviewed channel (flag in report).

## SHIPPED — OPS+CONTENT+SECURITY
self-improve(edit-type clustering) + sw-writer(rolling frame memory) + sw-editor(numeric-precision + frame-log + rewrite delta) + sw-marketing(hook-not-recap + discovery curation) [commit 8cb2f81]. kerri-bot: hardcoded xoxb- token (account_inactive, confirmed via auth.test) -> ~/.kerri-chief/secrets/kerri-slack-bot.env, skill sources it; node-inherits-env verified [local skill file, not in brain git].

## VERIFIED-ALREADY-DIALED (audit over-reported; NOT edited)
eod-meetings orphan-dedup (STEP 0 already reads jobs.json + deterministic external_ref); lead-research per-run lane balance (already in digest — shipped the cross-run drought guard instead); sw-writer recycled-frame (STEP 7.5 critic already does it — extended to multi-issue memory).

## COMPOUND
1 consolidated Learning appended to savant-build-learnings.md [commit f8e5d76]; brain/log.md line prepended. Key compounding insight -> 2 proposed gap-sweep checks (retired-endpoint reference scan; inline-credential scan).

## PROPOSE-ONLY -> Brian (report task 70d83497, action_needed)
1. 🔴 morning-brief STEP 2 reads GET /api/v1/revenue_command = 404 CONFIRMED (deals=200). Fix ready: read /api/v1/deals. SEND-AUTHORITY -> needs Brian OK.
2. kerri-bot token reissue (account_inactive).
3. kerri-skill pre-send repeat-mistake pattern gate. SEND-AUTHORITY.
4. event-planning email-sequence + run-of-show templates (dedicated build). gap-sweep retired-endpoint + inline-credential scans (dedicated build).

## RUN COMPLETE — 2026-06-18 15:54 EDT
11 routines/skills improved across 3 thematic commits + 1 security fix + 1 Learning. 0 external sends. 0 invented data. Send-authority rail held (morning-brief/kerri-skill = propose-only). Report filed + verified (task 70d83497). NOW.md In-flight updated (did not clobber concurrent sweep's Last-touched baton). Pre-existing iCloud->Projects path-fixes left for the nightly sync (kerri-skill stays for reviewed channel).

## FOLLOW-UP — morning-brief 404 fix SHIPPED (2026-06-18 16:36 EDT)
Brian approved in-thread ("Yes, apply now"). morning-brief STEP 2 repointed off the dead /api/v1/revenue_command (404) to GET /api/v1/deals (value/weighted_value/stage/fulfillment_summary) + hwfyi-revenue-goal-sheet.mjs --pipeline-summary scoreboard helper; Sheet stays as cross-check mirror; retirement documented inline. Commit 29cc0c5 (isolated send-authority reviewed commit). Verified: revenue_command 404 / deals 200 / scoreboard booked $526,372 open $50,000 weighted $32,000 byStatus canonical. Takes effect next weekday 06:57 ET run. Report task 70d83497 item 1 = RESOLVED; items 2-4 still open for Brian.

---

# Build Loop — DEEP WRITING-CRAFT PASS — 2026-06-18 16:47 EDT (unsupervised)

Goal (Brian /build-loop): become S-tier at two writing capabilities through extensive research, then wire the craft into the agents that execute. (1) Greatest-possible B2B sales-email + proposal drafter (SDR craft). (2) Most creative B2B business + newsletter writer (Standard & Works). Per item: research → plan → write a master craft playbook → VERIFY with real before/after sample outputs scored against an explicit rubric (looped draft→critique→revise until S-tier) → wire into the drafting routines/playbooks.

Distinct from today's ~15:55 "skills & routines compound pass" (a breadth audit that added small functional guards across ~30 routines). THIS pass is DEPTH on writing craft, building ON the existing `lead-writing-playbook.md` and `hwfyi-package-quote-playbook.md`, not undoing the 15:55 guards.

Repo: `~/Projects/kerrios-agent-brain/`. NOT a Rails run — "ship" = commit a craft playbook to the brain + VERIFY via a real before/after sample that clears the rubric. No prod deploy.

MODE: **UNSUPERVISED** (session-transcript + interactive-approval tools returned "unavailable in unsupervised mode"). Brian fired this off and stepped away → strict overnight rails even though clock is daytime.

Hard rails this pass:
- NO external sends. NO invented data (no fabricated prices/metrics in sample sales drafts — use already-sent REAL examples, e.g. the Modelwise gold-standard quote, as the canvas).
- NO edits to send-authority files (`kerri-inbox-sweep/SKILL.md`, `kerri-skill/SKILL.md` + `references/email.md`, `kerri-morning-brief/SKILL.md`, `data/autonomy-policy.json`) → PROPOSE only, surfaced in the report.
- The S&W newsletter writer/editor/marketing prompts are NOT send-authority → editable with an explicit reviewed commit (the 15:55 precedent, commit 8cb2f81). The sales-pod routines (cold-outreach, lead-research, pipeline-followup, build-proposal) are also editable.
- S&W BOUNDARY: all newsletter craft stays in `brain/.local/` (gitignored), never pushed to Savant/synced-brain/GitHub. The HWFYI sales playbook is KMG side → synced brain, pushable.
- Research = PUBLIC writing technique only.

## SETUP — 16:47 EDT
- Crash-recovery: today's ledger holds 3 completed runs (gap-pipeline overnight, mobile-app daytime, 15:55 skills-compound) + the 16:36 morning-brief 404 fix. Nothing in flight collides; this is run #4, appended below.
- Grounded on: `lead-writing-playbook.md` (built THIS morning from copywork of Levine/Hobart/Doomberg/Potter/McCormick — i.e. the morning newsletter-edit session's distilled output), `hwfyi-package-quote-playbook.md` (quote shape + discount ladder + never-negotiate-price), `definition-of-done-gate.md` (7-step pre-send gate), and the finalized 6/18 issue `~/Downloads/2026-06-18-FINAL.md` (current quality bar).
- Morning iMessage/terminal transcript Brian referenced: the literal session-transcript tool requires interactive approval (unavailable unsupervised). Its OUTPUT is already on disk (the playbook + the FINAL issue) and is used as the bar; flagged in the report.
- Research: launched 5 background agents — SALES: (S1) cold-email canon [DONE], (S2) persuasion + value-proposals [DONE], (S3) B2B copy craft [running]; NEWSLETTER: (N1) whole-issue craft [DONE], (N2) exemplar bullet-level technique [running]. Each returns imitable technique + micro-examples, house-rules-compliant.
- Order: Item 1 (sales) ships fully before Item 2 (newsletter). Research prep is parallel; shipping is sequential.

## ITEM 1 — SALES EMAIL + PROPOSAL MASTERY

### Phase 1 — BRAINSTORM (16:50 EDT)
Approaches to encoding world-class SDR craft:
1. **One master `hwfyi-sales-writing-playbook.md` (synced brain)** = operating philosophy (cross-linked, not duplicated) + cold first-touch craft + the persuasion/value engine + the proposal narrative arc (links the package-quote-playbook for package shape) + a pre-send SALES rubric + worked before/after examples. Drafting routines reference it. **CHOSEN** — single source of craft truth, additive, slots into the existing playbook ecosystem, editable without touching send-authority files.
2. Embed the craft directly in inbox-sweep DRAFTING. REJECTED — send-authority (propose-only unsupervised); bloats the runtime prompt; couples craft to one routine.
3. Fragmented micro-references. REJECTED — fragmentation is the exact failure the definition-of-done-gate consolidated; one playbook beats five rules.
Data check: examples use REAL already-sent context (Brian's Modelwise gold-standard quote; real queued prospects) with NO invented prices. Cold opener uses audience-fit-first framing (no product pitch on the opener) per the first-touch rule.

### Phase 2 — PLAN
- WORK: synthesize S1+S2+S3 → write `brain/wiki/workflows/hwfyi-sales-writing-playbook.md`: (a) philosophy (value-first / audience-fit-first / never-negotiate-price, cross-linked), (b) cold first-touch craft (opener patterns, relevance-not-personalization, one interest-CTA, length/format/mobile, subject lines, follow-up cadence, reply-killers), (c) the persuasion/value engine (Cialdini/Voss/Challenger/Enns → sentence-level moves), (d) the proposal narrative arc (their world → gap/cost-of-inaction → bridge → options → low-friction next step; links package-quote-playbook), (e) a 10-point pre-send SALES rubric, (f) worked before/after examples.
- VERIFY (proof): produce 2 real before/after samples — a cold first-touch to a real queued prospect (no invented data) and a post-call quote cover-note around Brian's REAL Modelwise packages — each scored against the rubric, looped draft→critique→revise until it clears. Run the presend-lint mental gate (em dash, AI tells, signature/identity).
- WIRE: add the playbook to the sales-pod routine references (cold-outreach, lead-research, pipeline-followup, build-proposal — editable) + PROPOSE a one-line pointer in inbox-sweep DRAFTING (send-authority → report).
- DONE (one sentence a non-engineer can verify): there is one HWFYI sales-writing playbook the drafting agents follow, and a fresh cold email + quote note written to it visibly beats the prior generic drafts against a 10-point rubric.

## ITEM 2 — S&W NEWSLETTER WRITING MASTERY

### Phase 1 — BRAINSTORM (16:52 EDT)
1. **EXTEND `lead-writing-playbook.md` (.local) into a whole-issue craft playbook** (or a sibling .local whole-issue file it links): keep the Lead section, add subject/preview craft, the "so what" bullet engine, section pacing, WATCH-line craft, the close, voice consistency, + a whole-issue rubric. Wire into the sw-newsletter writer/editor/marketing prompts. **CHOSEN** — builds on this morning's fresh foundation, respects the S&W boundary (.local), and those prompts are NOT send-authority (reviewed commit).
2. New separate whole-issue page disconnected from the Lead playbook. REJECTED — splits craft from what the writer already loads.
3. Only edit the prompts, no playbook. REJECTED — craft belongs in a referenced playbook so the critic pass can check against it.
Boundary: all S&W newsletter craft stays in `brain/.local/`; research is public technique only.

### Phase 2 — PLAN
- WORK: synthesize N1+N2 → extend the lead playbook into whole-issue craft: subject-line/preview patterns, the "so what" bullet (report→frame consequence clause), section pacing/scannability (bold the lead noun, consequence-order), WATCH/indicator craft (level + move + threshold), the close (land a dry true line, don't summarize), voice through-line; + a whole-issue pre-delivery rubric folding in the house hard rules.
- VERIFY (proof): take the 6/18 FINAL issue as the "before" bar; produce sharper "after" versions of (a) 3 sector bullets and (b) the subject/preview line, scored against the rubric, looped until they clear.
- WIRE: fold the craft + rubric into kerri-sw-newsletter-writer (write to it), -editor (critic checks against it), -marketing (subject/preview). Reviewed commit; .local craft committed locally, not pushed.
- DONE: the newsletter writer/editor follow a whole-issue craft playbook + rubric, and rewritten sample bullets + subject line visibly beat the 6/18 issue against it.

(Phases 3-6 logged as executed below.)

---

## ITEM 1 — EXECUTED

### Phase 3 — WORK ✅
Wrote `brain/wiki/workflows/hwfyi-sales-writing-playbook.md` (new) synthesizing the 3 sales research streams into one craft reference: stance cross-links (value-first / audience-fit-first / never-negotiate-price, linked not duplicated); PART A cold first-touch (the open-on-their-world structure upgrade, 5 opener patterns, relevance-that-scales, the one-CTA hierarchy offer>interest>time, length/grade/mobile limits, two-word subjects, follow-up cadence, the 8 reply-killers); PART B persuasion+value engine (Cialdini/Voss/Challenger/Enns → sentence-level moves + price-objection handling); PART C the quote narrative arc + 3-option anchoring (links package-quote-playbook for the package shape, no duplication); PART D sentence craft (Wiebe/Shleyner/Handley/Ogilvy); the 10-point pre-send SALES rubric; three worked before/after examples (cold first-touch, price-objection reply, quote cover-note using Brian's REAL Modelwise packages — no invented prices). Status = canonical-candidate (the opener change to the live routine is proposed).

### Phase 4 — REVIEW ✅
- Deterministic lint (mirrors the pre-send gate): em dashes appear only in section headers + wiki-link glosses (the convention every existing brain page uses); the worked OUTBOUND email examples are em-dash-free. ZERO negation-formula AI tells in prose. Dead follow-up phrases appear only inside the reply-killer ban list. PASS.
- Hostile self-review: the playbook cross-links existing rules rather than duplicating/contradicting them; the proposed opener upgrade is explicitly fenced as pending Brian, so the wired routines never get contradictory guidance.

### Phase 5 — VERIFY (before/after proof, looped) ✅
Scored the AFTER cold first-touch against the 10-point rubric item by item: opens on the buyer's hiring trigger (1✓), one concrete relevance token (2✓), audience-fit not product (3✓), single value-offer CTA (4✓), ~62 words at grade ~5 (5✓), neutral two-word subject (6✓), value in the buyer's terms not display (7✓), n/a price (8✓), no em dashes / AI tells (9✓), Kerri sign-off + "lead partnerships" title (10✓). The BEFORE (current cold-outreach STEP 4 template) fails items 1, 2, 3, 4: it opens about us, gives a generic fit, asks for a soft "conversation." The after demonstrably beats the before. Loop converged (one optional line-2 tightening noted; already clears the bar).

### Phase 5b — SHIP (wiring, reviewed commit) ✅
- `kerri-cold-outreach/SKILL.md`: added the playbook to the read-only reference list + a STEP 4 "run the pre-send rubric" instruction applying items 2-10 now and explicitly deferring item 1 (the opener flip) to Brian. Additive, non-contradictory.
- `kerri-pipeline-followup/SKILL.md`: added the playbook to the read-only references with Part A7 + Part B + Part D + rubric guidance for warm nudges and reply handling.
- Both are NON-send-authority files; no outbound wording that currently sends was changed.

### Phase 6 — PROPOSED (not auto-applied; surfaced in the report)
- HIGH-IMPACT: flip cold-outreach STEP 4's opener from "I'm Kerri / We're a media company" (opens about us, reply-killer #1) to the playbook's open-on-their-world four-beat (exact before/after in the playbook's Worked section). Needs Brian's one-word yes (changes outbound wording he authored; held under the unsupervised send-authority-adjacent caution).
- Add the same one-line playbook reference to kerri-lead-research (hook craft) + kerri-renewal-watchdog (renewal drafts), and a pointer in the inbox-sweep DRAFTING section (send-authority → propose).

DONE ✅ — one HWFYI sales-writing playbook the drafting agents load, and a fresh cold email written to it beats the prior generic template on the 4 of 10 rubric points that template fails. Commit below.

---

## ITEM 2 — EXECUTED

### Phase 3 — WORK ✅
Extended the canonical, routine-loaded `agent-prompts/kerri-skill/references/sw-lead-writing-playbook.md` (the file the writer + editor actually load — confirmed by grep; the brain/.local copy my memory pointed at was an orphaned earlier draft) with PART 2 — THE WHOLE ISSUE: subject/preview craft, the "so what" bullet (end on a source-supported consequence clause, one fact one move), number-framing for bullets (yardstick / delta / second-order), the five exemplar moves compressed to bullet length, section pacing + scannability (consequence-order, bold lead noun, 20-sec skim), the FLOOR/WATCH craft, the close, the voice through-line, a 10-point whole-issue pre-delivery rubric, and worked before/after on REAL 6/18 bullets. Synthesizes N1 (Smart Brevity/Axios/Stratechery/Morning Brew) + N2 (Levine/Hobart/Doomberg/Potter/Packy bullet-level). Also de-staled the title "S&W Industrialist" → "Standard & Works Newsletter" (the newsletter-name rule). Built on the existing Part 1 + Brian's anti-slop note; did not touch them.

### Phase 4 — REVIEW ✅
- Deterministic lint (.local draft + canonical references copy): em dashes only in the PART 2 header (brain-doc convention); AFTER example bullets em-dash-free + exclamation-free; ZERO negation-formula AI tells in prose. PASS.
- Hostile review: PART 2 is additive to Part 1 (no contradiction); the editor wiring is FLAG-not-fabricate (it cannot invent a "so what" the source does not support — protects the freshness/accuracy bar); the writer writes the consequence from the source.

### Phase 5 — VERIFY (before/after on real 6/18 bullets, looped) ✅
The 6/18 issue is already strong, so the proof is the upgrade margin. Took 3 real bullets that END ON A BARE FACT (the precise failure PART 2 fixes) and rewrote them to end on a "so what":
- Nokia: ended "backed by the Shapiro administration" → ends on the incentive ("a bet that customers will pay a premium for advanced packaging that never routes wafers through Asia").
- C.H. Robinson: ended on scale stats → ends on what BidBoardX changes ("pulling the contract-versus-spot decision that sets truckload margins onto its own platform").
- Saxum: ended on the facility description → ends on the structural read ("a generational bet that West Coast import food keeps moving through fixed dockside capacity").
Each AFTER passes the whole-issue rubric; the BEFOREs fail item 2 (end on the bare fact). Demonstrable improvement vs the bar; loop converged.

### Phase 5b — SHIP (wiring, reviewed commit) ✅
- references/sw-lead-writing-playbook.md: PART 2 + rubric appended; title de-staled. (Loaded by writer + editor; NON-send-authority.)
- kerri-sw-newsletter-writer: DATA FILES description → Lead + whole issue; STEP 5 gains the "end on the so what" bullet craft.
- kerri-sw-newsletter-editor: INPUTS updated; STEP 4 gains the whole-issue rubric check (flag-don't-fabricate).
- brain/.local/.../lead-writing-playbook.md (orphan): reduced to a pointer at the canonical file to kill duplicate-drift. Gitignored, not committed.
- kerri-sw-newsletter-marketing: NOT edited — already opens-on-a-hook / discovery-not-recap (the 15:55 pass dialed it); aligned with the research.

### Phase 6 — note
The "Industrialist" label still appears in writer/editor prose + the voice filename (voice-sw-industrialist.md). Renaming the file would break references; left as a flagged hygiene follow-up, not in scope tonight.

DONE ✅ — the newsletter writer + editor now load a whole-issue craft playbook + 10-point rubric (not just a Lead playbook), and 3 real 6/18 bullets rewritten to it beat the shipped versions on the "end on the so what" rule. Commit below.

---

## WIRING FINALIZED + RUN COMPLETE — 2026-06-18 PM (see commit timestamps)

Sales playbook wired into the THREE routines that actually draft sponsor prose: kerri-cold-outreach + kerri-pipeline-followup (commit 53a2c90) and kerri-renewal-watchdog (drafts renewals in STEP 3; commit ac0c338). kerri-lead-research deliberately NOT wired — it finds leads and explicitly "does NOT draft emails" (its SKILL line 8); cold-outreach applies Part A3's relevance lens to the hooks it selects. Same drafter-vs-not discrimination that correctly excluded kerri-build-proposal (which composes internal build-run proposals, not sales prose).

Commits this run (all on main, pushed):
- 53a2c90 — sales-writing playbook (new) + cold-outreach + pipeline-followup wiring.
- e11023b — newsletter whole-issue Part 2 in the canonical references playbook + writer/editor wiring + title de-stale.
- 00e4cc2 — consolidated Learning (savant-build-learnings) + log + NOW handoff.
- ac0c338 — renewal-watchdog wiring + playbook header truth.
Local-only (gitignored, not pushed): brain/.local lead-writing-playbook reduced to a pointer; Claude memory feedback_sw_lead_quality + MEMORY.md repointed off the orphan to the canonical references path + the sales twin.

Both items SHIPPED + VERIFIED (a real before/after that fails the rubric in the BEFORE and clears it in the AFTER). 0 external sends. 0 invented data. S&W boundary held (craft in committed references/, pre-publish drafts gitignored in .local). Send-authority rail held: PROPOSE-only on the two items below. 0 items FAILED, 0 PARKED, 0 NOT STARTED.

REMAINING FOR BRIAN (Console report task d7304d47, action_needed):
1. HIGH-IMPACT: approve the cold-outreach STEP 4 opener flip (open-on-their-world vs open-about-us, the #1 reply-killer in the data). One yes and I flip it.
2. Add the inbox-sweep DRAFTING pointer (send-authority file → reviewed edit).
3. Optional: de-stale the "Industrialist" naming in the sw prompts + the voice-sw-industrialist.md filename.


================================================================
# BUILD LOOP RUN 4 — Savant "Monaco" design migration + Ask Savant agent
Started: 2026-06-18 ~20:05 ET (interactive, Brian via /build-loop)
Runner: Kerri (Claude Code). Repo: kerrihq-rails. Session-start main SHA: c676903.
Work branch: savant-monaco-redesign (merge to main to deploy once Brian green-lights the visual).

## Brian's directive
Execute the Monaco-inspired design (the 2026-06-18 concept: pure-black canvas, editorial
serif display, floating pill nav, monochrome + violet→magenta accent, calm "morning brief"
home) across ALL of Savant. Keep the DATA whole; change the LAYOUT (cleaner than the current
dense terminal look). Then wire a Claude SDK agent ("Ask Savant") that lives on the site.
Run the loop until Savant looks like the future of a media company's OS.

## Setup recon (done)
- Savant is ALREADY a single dark, class="dark" theme driven by shared tokens
  (tailwind.config.js: ink/line/fg/accent/ok/warn/err) + shared CSS component classes in
  application.tailwind.css (.panel, .console-btn, .microlabel, .nav-item, .badge, .kanban-*,
  .mobile-bottom-nav) + a global shell (shared/shell/_sidebar, _sidebar_nav, _topbar,
  _mobile_bottom_nav, _command_palette). Fonts self-hosted: Inter + IBM Plex Mono. Accent blue #4C90F0.
- So the migration is dense-terminal -> calm-editorial, NOT light->dark. One foundation change
  (tokens + shared classes + shell) propagates to every screen; per-screen work is then layout polish.
- ~15 screen groups (Overview/root, Tasks, Approvals, Pipeline worklist, Deals kanban, Renewals,
  Knowledge/Brain, Newsletter, Revenue, Events, Companies, People, Agents, Sources, Sponsor portal).
- Test infra: RSpec + FactoryBot; system specs use selenium headless_chrome at [1440,*] and [390,844],
  sign_in via Devise helper; save_screenshot available (verification path for the visual).
- anthropic gem ~1.25 already in Gemfile + agents/agent_runs models/views exist -> foundation for the agent step.

## BRAINSTORM (3 approaches)
A) Evolve the shared tokens + component classes + shell toward Monaco, then refine each screen. CHOSEN.
   Lowest blast-radius-per-impact: one foundation edit re-skins the whole app because every screen
   reuses the same classes; remaining work is per-screen layout polish. Strictly view-layer + additive.
B) Parallel "v2" theme behind a data-theme flag + duplicate shell with a toggle. REJECTED: doubles the
   surface, invites drift, more code; the app already has exactly one theme to evolve, and Brian uses one prod surface.
C) Rewrite each screen's markup bespoke, ignoring shared classes. REJECTED: enormous, inconsistent,
   high regression risk, slow.

## CHOSEN APPROACH
Evolve foundation first (tokens: pure-black ink + violet/magenta accent; add a self-hosted serif +
display heading styles; restyle .panel/.console-btn->pill/.microlabel->sentence-case/.nav-item; add
.pill/.pill-grad/.ask-bar/.glass; restyle the shell to a calm pill-nav feel). Verify on the Overview
screen with a real screenshot, show Brian, then iterate screen-by-screen reusing the evolved classes.
HARD CONSTRAINTS for the design items: presentation only. No migrations, no model/controller/route
changes, no API field renames/removals (agents poll /api/v1 around the clock), no destructive DB,
no external sends, no send-authority files. Data, controllers, instance vars stay identical.

## PLAN — ITEM 1: Foundation + Overview (this run's first shippable unit)
1. Serif: self-host (or Google Fonts link) an editorial serif; add font-serif to tailwind + @font-face;
   add a .display heading utility.
2. tailwind.config.js: re-point ink toward pure black; change accent ramp to violet (#8161FF family) + add magenta token. Keep ok/warn/err/prop.
3. application.tailwind.css: restyle .panel (air, softer), .console-btn + new .btn-pill (white pill primary),
   .microlabel (lighter, sentence-case), .nav-item; add .pill, .pill-grad (violet->magenta), .ask-bar, .glass, .greeting hero.
4. Shell partials (_sidebar/_sidebar_nav/_topbar): calm Monaco chrome, serif wordmark; keep all links + active-state logic.
5. dashboard/index.html.erb: reflow to the morning-brief — serif greeting hero + Ask Savant bar + "Needs you"
   cards + calmer revenue/agents/events sections. Keep every instance var (@attention/@booked/@goal/@open_pipeline_value/@today/events).
6. Build CSS (yarn build:css) + JS if needed.
REVIEW GATES (all): bin/rubocop -a + clean rubocop; full bundle exec rspec green; bin/brakeman no new warns;
hostile self-review of the diff.
VERIFY: Capybara screenshot of Overview at 1440 + 390 (real app, seeded), show Brian.
DONE (non-engineer check): "Overview loads looking like the Monaco concept — serif greeting, Ask bar,
clean cards — with all the same data, and nothing else broke."

## BACKLOG (subsequent items; each = own brainstorm/plan/work/review/ship/compound)
 2. Tasks board (kanban) reskin
 3. Approvals + Approval requests
 4. Pipeline worklist + Deals kanban
 5. Companies + People (CRM)
 6. Knowledge records (Brain)
 7. Renewals + Revenue
 8. Newsletter inventory + issues
 9. Events + Sponsor portal
10. Command palette + mobile bottom nav polish + login screen
11+. ASK SAVANT AGENT (Claude SDK): additive read-only endpoint backed by the anthropic gem + agents infra,
    grounded in knowledge_records + CRM, streamed into the ask bar; any action approval-gated. Multiple items.

## PHASE LOG
- 20:05 ET SETUP done (recon + branch + ledger). Next: PHASE 3 WORK on Item 1 foundation.
- 20:30 ET ITEM 1 WORK+REVIEW done. Foundation (tokens/serif/component classes) + shell serif wordmark + Overview reworked to the morning-brief. Visual verified via a system-spec screenshot (desktop+mobile, real app + seeded data); shown to Brian who chose "ship now, keep grinding". GATES: rubocop clean (696 files), rspec 1652 ex / 0 fail / 1 pending, brakeman 0 warnings. Commit d4ddfda on main (ff-merge of savant-monaco-redesign). Render deploy dep-d8qaum0js32c73f62mog build_in_progress. NEXT: confirm live + prod verify, COMPOUND a Learning, then Item 2 (Tasks board kanban).
- 20:45 ET TRIAGE (Tasks/Pipeline/Pipeline-worklist/Renewals/Revenue/Companies after the foundation): the foundation already carried the Monaco look to every core screen cohesively (near-black canvas, violet accent + probability bars, serif wordmark, clean panels/cards/kanban). "Design across the board" is substantially DONE via item 1. Remaining design = marginal editorial polish (serif page H1s, sentence-case a few eyebrows) + peripheral legacy views (teal/gold primary/secondary) — deferred as optional. PIVOT to the centerpiece: ITEM 2 = live "Ask Savant" Claude agent wired into the Overview ask bar.
- 23:10 ET SIDE-FIX (Brian: "dragging a deal doesn't work"): kanban drag was handle-only + native HTML5 DnD (grabbing the body opened the deal; native DnD often started no drag). Fixed to whole-card + SortableJS mouse fallback (forceFallback + fallbackTolerance), tasks board unchanged. Verified in a REAL browser: deal lead->qualified persisted; update_stage PATCH 200+turbo-stream; visual screenshot confirms card moved. Caught + purged 24 phantom test failures from a leftover temp system spec (db:test:prepare); full suite 1655/0. Commit 76b520b on main, deploying. Learning recorded.
