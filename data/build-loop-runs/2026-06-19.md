# Build Loop Run — 2026-06-19

Task list: `data/build-loop-runs/PROMPT-savant-actions-and-access.md`
Goal: (1) give Ask Savant the power to ACT in-app, (2) stand up multi-user siloed access with the exact visibility matrix and make the agent respect it.

Repo: `~/Projects/kerrihq-rails` (Savant). Deploy: push `main` → Render auto-deploys.
Start: 2026-06-19 00:48 ET. Hard stop ~06:30–07:00 ET. Repo clean on `main` at HEAD 42ce4c3.

## Access matrix (the spec)
| Capability | Brian | Ari | Benji | Zach |
|---|---|---|---|---|
| Own profile + tasks, fully siloed | yes | yes | yes | yes |
| See ANOTHER person's tasks | NO | NO | NO | NO |
| Pipeline (deals/revenue/worklist) | yes | yes | yes | NO |
| Ironclad Maritime Summit | yes | yes | NO | yes |
| Ask Savant (scoped to their access) | yes | yes | yes | yes |

People: Brian owner (brian@kerrihq.com), Ari CFO (ari@kerrihq.com), Benji CDO (benji@hardwarefyi.com), Zach S/W external (zach@standardandworks.com).

## Hard rails this run
- Security is the deliverable. Every access check FAILS CLOSED. New resources default to deny.
- Do NOT break `/api/v1` automation (KERRIHQ_AGENT_API_KEY / inbox sweep). Per-user siloing is for HUMAN users + their agent sessions; system/API-key access keeps current scope.
- API backward-compatible: only add fields, never rename/remove.
- Every agent action writes an audit record (AgentRun or equiv): actor, action, target, before→after, ts.
- NO external sends of any kind. Provisioning = records + grants only; invites are documented, not sent.
- S/W boundary holds.
- Additive + reversible. Ship one complete item at a time; roll back on any failure.

---

## Phase log

### 00:48 ET — SETUP
- Read PROMPT, build-loop SKILL (canonical), savant-build-learnings.md, NOW.md, last log lines.
- Env confirmed: repo clean on main @ 42ce4c3; Postgres@17 running; PATH export needed per Bash call.
- Ordered items per PROMPT (safest first): 1 access foundation → 2 siloed tasks → 3 pipeline gating → 4 Ironclad restriction → 5 provision teammates (records only) → 6 Ask Savant reads scoped → 7 Ask Savant actions → 8 (stretch) my-work.
- Next: deep recon of the real data model before any code (auth/API-key path, permission primitives, tasks, deals/events/Ironclad, AskSavant internals).

### 00:54 ET — RECON COMPLETE (5 full-read agents) — key findings
**Topology (decisive):** Production is SINGLE-tenant — ONE Organization (`c7ec4a59…`); all four humans are members of it. "Hardware FYI" / "Standard & Works" are **Domains** (brain layer), not orgs. Ironclad (`7f20c77d…`) is an `events` row under the one org. So BOTH pipeline-gating (hide from Zach) and Ironclad-restriction (hide from Benji) are restricted-data-WITHIN-one-org problems.

**Three auth paths:** (1) human web → Devise `current_user`, `current_organization = current_user.organizations.first`, Pundit user = plain `User`. (2) JSON API `/api/v1` → `ApiKey`→`ApiPolicyContext` (blanket org-admin, NO `current_user`) — this is the inbox-sweep path; **per-user gating lives ONLY in the User branch so the API is unaffected by construction.** (3) MCP → `Current.api_key`. ⇒ The safe seam: tighten the **human/User** path; never touch `Api::V1` controllers or `ApiPolicyContext`.

**Reusable primitives (do NOT reinvent):** Pundit policies fail-closed by default; `EventAccess`+`event_guest` = the per-(user,event) ACL (but ADDITIVE for outsiders — full members bypass it); `PermissionGrant`+`PermissionResolver` = grant-based domain×sensitivity brain ACL (master access = super_admin OR org owner); `AgentRun` = audit table with `actions_executed`/`records_changed`/`input`/`output`/`initiated_by` (no `ask-savant` agent seeded yet → must seed one).

**Tasks:** `tasks.assignee_id` nullable; board uses `scope :visible_to ->(u){ where(assignee: [u, nil]) }` ("my lane + unassigned strays"). API-create + AgentHealthCheckJob create UNASSIGNED tasks → strict siloing by assignee would hide every sweep card. Human surfaces (all use `visible_to`): tasks#index + set_task, `_column_counts.html.erb`, approvals#index, dashboard (×2), ConsoleStatus, ConsoleToday, AskSavant `list_tasks`. API surfaces to NOT touch: `Api::V1::TasksController`, MCP `list_tasks`.

**Pipeline:** boards/worklist/renewals already gate `authorize @organization, :show?` = full-member (event_guest blocked). Revenue already domain-gated. Gaps: dashboard org-level $ numbers + command-palette links (unconditional) + AskSavant pipeline tools. ⇒ pipeline access == full membership; external partner = event_guest (auto-excluded). No new pipeline grant needed for the 4 people.

**Ironclad:** EventAccess can't subtract an event from a full member (Benji) without demoting him org-wide. ⇒ add `events.restricted:boolean default:false`; for a restricted event, full members ALSO need an EventAccess (allow-list) row; event_guest branch unchanged. Cascade to `DealPolicy` (exclude restricted-event deals from non-allowed full members), `SponsorshipOptionPolicy`, `TicketSearchPolicy`, and FIX `TicketSalePolicy` (no event check today).

**Ask Savant:** `@user` already threaded into `ToolExecutor` but unused for scoping (every tool is org-only). Controller currently 403s sponsor-only AND event_guest → must allow event_guest (for Zach) and scope every tool. Seed an `ask-savant` Agent for audit attribution.

**Seeds run on every prod deploy** (render.yaml preDeploy `db:seed`) → seeds.rb IS the idempotent prod provisioning mechanism. MUST verify what it currently creates for ari/benji/zach before item 5.

### Chosen approach per item (value-to-blast-radius)
1. **Foundation (pure additive):** `events.restricted` migration; `User#can_see_event?` / `can_see_pipeline?`; task strict-ownership scope; the comprehensive permission-matrix spec (4 role factory users). No wiring → zero behavior change; verified by spec + /up.
2. **Siloed tasks:** backfill nil-assignee→owner; default assignee=owner on API-create + AgentHealthCheckJob (additive); flip `visible_to` to strict per-assignee (even owner/super-admin); route AskSavant `list_tasks` strict. Preserve `Api::V1` + MCP full-org. Guard: existing api/v1/tasks specs stay green.
3. **Pipeline gating:** gate dashboard org-$ numbers + command-palette/mobile nav behind full-member; request-spec matrix proving event_guest blocked from /deals,/pipeline,/renewals,/revenue + no $ on dashboard. API unchanged (system scope).
4. **Ironclad restriction:** flag Ironclad restricted + grant Brian EventAccess in the SAME data migration (no orphan window); EventPolicy show?/Scope + DealPolicy/SponsorshipOption/TicketSearch/TicketSale gating; specs Benji-excluded / Brian-Ari-Zach-included.
5. **Provision teammates (records only):** via idempotent seeds.rb — Ari/Benji full members; Zach event_guest (Ironclad-only, NO brain grants); EventAccess Ari+Zach→Ironclad. Document Brian's interactive invite step; send nothing.
6. **Ask Savant reads scoped:** allow event_guest; route find_deals/pipeline_summary behind pipeline access, list_tasks strict-per-user, search_brain through PermissionResolver. Prove no cross-matrix leak.
7. **Ask Savant ACTIONS (slice 2):** update_deal / create_task+update_task (self) / log deal note — each authorized as current_user via Pundit (fail-closed), permitted resources only, AgentRun audit row per action, confirm step for consequential changes, NO external sends. Verify end-to-end with real Ollama key + non-permitted user refused.
8. **Stretch:** my-work personalization.

Deploy strategy: serialize, but land item 4 (Ironclad policy + Brian access) and item 5 (provision Ari/Zach access) close together so Ironclad is never orphaned. Verify each behind-login change via request-spec matrix + /up + a read-only prod `rails runner` asserting provisioned access.
- Next: read ground-truth files (seeds.rb, user/task/event models, the 4 policies, application_controller) then implement Item 1.

### 01:05 ET — BASELINE GREEN
`bundle exec rspec` = 1666 examples, 0 failures, 1 pending. `bin/rubocop` clean (705 files). `bin/brakeman` 0 warnings. Clean foundation to build on. Read seeds.rb (runs on EVERY prod deploy → IS the idempotent prod provisioning mechanism; currently makes brian=owner, ari/benji/zach=member + brain grants. Zach is a FULL MEMBER today = over-exposed vs target; he must become event_guest. Zach owns the S/W newsletter agents → can't strip his brain grants without breaking AgentScopeResolver intersection → instead gate Ask Savant brain tool to full-members).

### 01:18 ET — ITEM 1 SHIPPED (committed local a2d1331; deploy batched w/ item 2)
BRAINSTORM/PLAN: foundation must be pure-additive (no behavior change) + the matrix spec is the verification deliverable. Approaches weighed: (a) central AccessMatrix service (rejected — over-engineered, breaks the `full_member_of?`-on-User idiom), (b) per-capability domain grants for pipeline (rejected — would need to grant every member a new grant + risks locking out members; in this codebase full-member already == sees pipeline), (c) helpers on User reused by policies/nav/tools (CHOSEN — idiomatic, lowest blast radius).
WORK: migration `events.restricted` (bool, null:false, default:false); `User#can_see_pipeline?(org)` = super_admin||full_member; `User#can_see_event?(event)` = restricted⇒needs EventAccess even for full members, else full-member-or-guest-with-access; both fail-closed on nil. Event factory `:restricted` trait. `spec/requests/access_matrix_spec.rb` (the matrix; item-1 cells = the helpers, both +/- for all 4 roles).
REVIEW/VERIFY: rubocop clean; matrix spec 5 green; full suite 1671 ex / 0 failures (+5). Zero wiring ⇒ zero behavior change. Prod-verify deferred to batched deploy.
- Next: Item 2 (siloed tasks) — backfill nil-assignee→owner, default assignee on API-create + AgentHealthCheckJob, NOT NULL, flip human surfaces to strict per-assignee (board/set_task/_column_counts/dashboard/approvals/ConsoleStatus/ConsoleToday/list_tasks), keep Api::V1 + MCP full-org. Then full suite + deploy items 1+2.

### 01:25 ET — ⚠️ CONCURRENT-SESSION COLLISION DETECTED + ISOLATED
Mid-Item-2 recon, the working tree had silently switched off `main` to `feat/person-partner-contact-type` with person.rb edits I never made. Diagnosed via reflog + `ps`: **a SECOND Claude session is running in the SAME working dir + same Postgres**, doing a "Person `partner` contact_type" feature (committed b630994 on its own branch, pushed to origin/feat/...) and running `bundle exec rspec` concurrently. It `git checkout`ed its branch in the shared dir right after my item-1 commit.
- **My work is SAFE:** local `main` still points at `a2d1331` (item 1), ahead of origin/main (42ce4c3) by 1. Nothing lost.
- **Hazard:** two sessions sharing one working tree + one test DB = working-tree thrash + phantom test failures + schema drift. Cannot safely proceed in the shared dir unattended.
- **DECISION (zero-damage + still-deliver):** isolate into a dedicated git **worktree** `~/Projects/kerrihq-rails-bl` on branch `bl/savant-access` off my local main (a2d1331), with a dedicated DB `kerrihq_rails_bl` via `DATABASE_URL`. Never touch the shared working dir, the other session's branch, or the shared `kerrihq_rails_{development,test}` DBs. Deploy via `git push origin HEAD:main` (fast-forwards origin/main → Render). Git object store + Postgres *server* are concurrency-safe; separate DB + separate worktree fully isolate the rest.
- **Known residue to report:** my earlier item-1 `db:migrate` ran against the shared `kerrihq_rails_development` DB before the collision was visible — added the additive `events.restricted` column there. Harmless to the other feature (additive), but noted for the morning report so it can be reconciled if they dump schema from that DB.
- Per-Bash from now on: `cd ~/Projects/kerrihq-rails-bl` + `DATABASE_URL=postgres:///kerrihq_rails_bl` + `RAILS_ENV=test`.

### 01:42 ET — ITEMS 1+2 SHIPPED + DEPLOYING (worktree-isolated)
ITEM 1 (635c35e) + ITEM 2 (f64be54) committed on `bl/savant-access`. Worktree isolation working cleanly; full suite 1682 / 0 failures (incl. system specs once gitignored assets were copied in), rubocop + brakeman clean.
- ITEM 2 detail: `Task.visible_to` flipped to strict per-assignee (drops the nil-stray leak) → flips board/set_task/_column_counts/approvals/dashboard/ConsoleStatus/ConsoleToday at once; `ConsoleToday#proof_events` siloed; `list_tasks` tool siloed; `TaskPolicy::Scope` strict (no caller, future-proof). No-orphan guarantee: `BackfillTaskAssignees` migration (nil→owner, verified owner>admin>member) + API-create & AgentHealthCheckJob default assignee to `Organization#default_task_assignee` (owner) + optional `assignee_email`. Api::V1 + MCP stay org-wide (system actors). Brian's queue preserved (his cards stay his). Test surgery: ~12 specs that created unassigned tasks acted-on-as-user got `assignee: user`; 3 nil-inclusion expectations rewritten to strict.
- CONCURRENT-SESSION note: origin/main moved twice under me — first to nothing-new (still 42ce4c3), then to `2ae39b7` (#122 queue-health badge, merged by the other session). Rebased my 2 commits onto #122 cleanly (auto-merged console_status_spec; verified console_status.rb still uses visible_to). Re-ran full suite green (1682) post-rebase. Pushed ff `2ae39b7..f64be54` → origin/main → Render deploy `dep-d8qdd828qa3s739d3fb0` building.
- Next: while deploy builds, implement items 3 (pipeline gating) + 4 (Ironclad restriction) in the worktree; verify 1+2 live (/up + API/automation-key unaffected) BEFORE committing/deploying 3+4.

### 02:00 ET — ITEMS 1+2 PROD-VERIFIED
Deploy `dep-d8qdd828qa3s739d3fb0` went LIVE (~3min). Prod checks: `/up` 200; `GET /api/v1/tasks?open=true` with the agent key → 200, 20 tasks ⇒ the inbox-sweep automation is NOT locked out (the #1 hard constraint holds). Live deploy ⇒ both migrations (events.restricted + backfill) ran in preDeploy. Items 1+2 have no user-visible change yet (siloing only bites once teammates exist), so this is the right verification depth.

### 02:30 ET — ITEMS 3+4 SHIPPED + DEPLOYING (`dep-d8qdmffaqgkc73atjah0`)
ITEM 3 (398caa5): pipeline (deals/worklist/renewals/revenue) already full-member-gated by OrganizationPolicy#show? + nav already hides Revenue/pipeline from event_guests; closed the one leak — dashboard "STALE DEAL" attention items now gated behind `can_see_pipeline?`. Matrix proves full members reach the boards, the external partner is redirected, and a stale KMG deal shows on a full member's dashboard but never the partner's.
ITEM 4 (ceb28c5): Ironclad restricted to allow-list (Brian/Ari/Zach), hidden from Benji. Two shared fail-closed ApplicationPolicy helpers (`event_visible?` + Scope `without_blocked_restricted_events`, Arel, no SQL strings); applied to EventPolicy + DealPolicy + SponsorshipOption/TicketSearch/TicketSale (last had NO event check). seeds flag Ironclad restricted + grant Brian access (no orphan window). Non-restricted events unaffected (helpers no-op when nothing's flagged) ⇒ backward-compatible.
  - 🔴 CRITICAL bug caught in hostile review (suite missed it — no test deal was restricted): `/api/v1/deals` authorizes as `ApiPolicyContext` (a non-User), which had no `can_see_event?` → the sweep touching an Ironclad deal would 500 in prod. FIX: added `can_see_event?`/`can_see_pipeline?` to ApiPolicyContext (org-admin sees all its org) + a regression spec (API serves a restricted-event deal). System/API access stays full by design.
  - Full suite 1689 / 0 failures, rubocop + brakeman clean. Pushed ff `f64be54..ceb28c5` → deploy building.
- Next: item 5 (provision teammates via idempotent seeds — Ari/Benji members, Zach→event_guest Ironclad-only; Ari+Zach EventAccess to Ironclad), document the invite step (don't send). Then item 6 (Ask Savant reads scoped, incl. brain to full-members so external Zach never sees the KMG brain) + item 7 (actions).

### 03:05–03:20 ET — ITEMS 5+6 SHIPPED; then PROD ONE-OFF EXPOSED A CRITICAL GAP
Items 5 (87b994f) + 6 (aa39801) committed + deployed (full suite 1692/0 each). Item 6 opened Ask Savant to event_guests with every read tool scoped (pipeline behind can_see_pipeline?+DealPolicy::Scope, brain full-members-only via PermissionResolver, list_tasks siloed).
**Then ran a read-only prod one-off (Render job, base64'd `rails runner`, output fetched from job logs).** MATRIXPROOF from PROD:
`restricted_col:true, nil_assignees:0, total_tasks:168, ironclad_name:"Ironclad Maritime Summit 2026", ironclad_restricted:FALSE, roles{brian:owner, ari:ADMIN, benji:ADMIN, zach:MEMBER}, ic_access{all false}, see_event{all true}, see_pipe{all true incl zach}`.
**🔴 ROOT CAUSE:** `db:prepare` (Render preDeploy) runs MIGRATIONS every deploy but only runs `db:seed` on a FRESH DB — so my item-4/5 SEED data (Ironclad restricted, Zach→event_guest, EventAccess) NEVER ran on the existing prod DB. Items 1+2 (migrations) ARE live (restricted col present, backfill done → 0 nil assignees). But items 4+5's DATA is absent ⇒ the access matrix is NOT yet enforced in prod (Ironclad still open to all members; Zach still a full member = over-exposed). The KerriOS-memory claim "seeds run on every deploy" is WRONG for an existing DB — the prior `db:seed:136` evidence was a coincidence/misattribution; the proven vehicle is a MIGRATION (per the tasks:resolve precedent).
**Also:** prod has Ari/Benji as ADMIN (manually promoted; admin ⊂ full_member, so the matrix still holds) — my item-5 seed `update!(role: :member)` would WRONGLY demote them + strip their deal-edit (DealPolicy#update? = admin). Must not force their role.
**FIX (in progress):** (a) revert seed ari/benji role-force; (b) add a guaranteed-to-run **data migration** `ProvisionTeammateAccess` (flag Ironclad restricted via ILIKE + demote Zach to event_guest + EventAccess brian/ari/zach; leave ari/benji untouched; idempotent + no-op on fresh dev/test). Deploy → migration applies to prod → re-verify with another one-off. THIS is what actually turns items 3/4/6's deployed policy code into an enforced matrix in prod.

### 03:30 ET — MATRIX FIX SHIPPED (8de18c5) + PROD-VERIFIED ✅ (the capstone)
Migration `ProvisionTeammateAccess` deployed LIVE. Re-ran the prod matrix one-off. POST-FIX MATRIXPROOF (PRODUCTION):
`restricted_col:true, nil_assignees:0, ironclad_restricted:TRUE, roles{brian:owner, ari:admin, benji:admin, zach:event_guest}, ic_access{brian:t, ari:t, benji:F, zach:t}, see_event{brian:t, ari:t, benji:F, zach:t}, see_pipe{brian:t, ari:t, benji:t, zach:F}`.
**The full access matrix is now ENFORCED IN PRODUCTION, exactly per spec:** pipeline = Brian/Ari/Benji not Zach; Ironclad = Brian/Ari/Zach not Benji; Zach = external partner (event_guest); Ari/Benji admin (full members) preserved; tasks backfilled (0 orphans). Items 1–6 live + verified.

### 03:55 ET — ITEM 7 SHIPPED (98df743): Ask Savant ACTIONS (slice 2)
update_deal (preview→confirm, DealPolicy#update?/admin + pipeline access), create_task (own board, self-assigned), update_task (own task only) — each authorized as current_user via the SAME Pundit policy (fail-closed), mutation+AgentRun audit in ONE transaction (failed audit rolls back), no external sends. find_deals/list_tasks now return ids. Lazily-created on-demand `ask-savant` Agent anchors the audit (never trips health check; seeds don't run on prod). SystemPrompt + drawer message updated.
- 🐞 caught: inside `module AskSavant`, `Agent` resolved to `AskSavant::Agent` (the loop class) not the model → `find_or_create_by!` undefined. Fixed to `::Agent`. (Spec surfaced it; blanket rescue had masked it as {error:}.)
- Full suite 1696/0, rubocop (.rb) + brakeman clean. **E2E with REAL glm-5.2:** agent called create_task → task persisted (self-assigned) → AgentRun audit row written (initiated_by=user, action=create_task, target=task:ID). Refusal (non-permitted user) proven by spec.
- Item 8 (stretch: my-work personalization): intentionally SKIPPED — optional, and remaining time is better spent on verification + the morning report + compounding (per build-loop).
- Next: verify item-7 deploy live; COMPOUND (build learnings); morning report (Console task) + NOW.md + brain log + push.

---
---

# Build Loop Run #2 — 2026-06-19 night (Pricing Intelligence)

Task list: inline (`/build-loop` invocation). Goal: ship a read-only **Pricing Intelligence** view under
Savant's Revenue nav that surfaces KMG's real demand curve (so quotes stop being guesses). Real closed
deals only. Zero invented numbers. Hard stop 07:00 ET (wrap 06:30). Started 23:33 ET.

## Setup (done 23:33–00:05 ET)

- **Isolation (per last night's collision rail):** dedicated git worktree `~/Projects/kerrihq-rails-bl`
  on branch `bl/pricing-intel`, based on **origin/main = 98df743** (the live production state; local
  `main` a2d1331 is 10 behind — prior run deployed via `git push origin HEAD:main`, so origin/main is
  ground truth). Dedicated test DB `kerrihq_rails_bl` via `DATABASE_URL=postgresql:///kerrihq_rails_bl`,
  stamped `RAILS_ENV=test`. Never touch shared `kerrihq_rails_{development,test}` or the main checkout.
- **Baseline green:** `spec/requests/pipeline_spec.rb` 5/5 in the isolated DB. 141 migrations all up.
- **Foundation verified present in 98df743:** `User#can_see_pipeline?` + `ApiPolicyContext#can_see_pipeline?`
  (API-actor gotcha already handled), `PipelineController`, `RenewalsController`, `DealPolicy::Scope`.
- **Prior learnings read:** PipelineController-mirrors-RenewalsController; rubocop chokes on .html.erb
  (lint .rb only); nav link in existing group ≠ new surface, key active-state on own path segment;
  DealBrief string-key ERB/JSON parity; worktree gotchas (empty builds dir, db:environment:set, re-fetch
  before push). No migration tonight (pure read) → seeds-vs-migrations learning N/A.

## Real production data recon (read-only API pull — the demand curve)

109 deals. **closed_won: 41** (all priced, $1K–$109K, median $10K; 40 sponsorship + 1 product).
**closed_lost: 7** (6 unpriced/zero, 1 priced $5K; all sponsorship).
Won size-band distribution (real): `<$5K: 4 · $5K–$10K: 16 · $10K–$25K: 13 · $25K–$50K: 6 · $50K+: 2` = 41 ✓.

---

### ITEM 1 — READ LAYER (Revenue::PricingIntelligence, no migration)

**BRAINSTORM (chosen).** Three approaches:
- (A) SQL GROUP BY + `percentile_cont` median — rejected: DB-specific median is brittle/hard to test;
  48 closed rows make in-memory trivial, no N+1.
- (B) **In-memory aggregation over a policy-scoped `deals.closed` relation (chosen)** — bucket in Ruby by
  (a) deal_type and (b) value-band; won/lost/win-rate per segment + min/median/max over priced-won only.
  Mirrors `Crm::*Brief` PORO+`call`+string-key shape. Respects last night's matrix (restricted-event deals
  the viewer can't see never enter the aggregate). Cheap, testable, zero invention.
- (C) Segment by sponsorship package (SponsorshipOption) — rejected as category axis: many-to-many → ambiguous
  per-deal package; won deals don't reliably carry selections → "no package" buckets imply missing data.
  `deal_type` is always-present (validated), one-per-deal, truly "category as it exists on the deal."

Chosen **(B)**. Category axis = `deal_type`; size axis = value-band. Win rate over closed deals in the segment;
price stats over closed_won with value present AND > 0 (null/zero excluded — phantom rule). Size-band only
places deals WITH a positive value; unpriceable closed deals (mostly the 6 lost) are NOT invented into a band —
surfaced as an explicit excluded count.

**PLAN.**
- `app/services/revenue/pricing_intelligence.rb` — `new(deals_relation)`, `#call` → string-keyed hash:
  `by_category[]`, `by_size_band[]`, `totals`, `excluded_unpriced_closed`. Segment row: key, label, won_count,
  lost_count, closed_count, win_rate (nil if no closed), priced_won_count, unpriced_won_count, price_min,
  price_median, price_max (all nil if no priced-won).
- Bands: `<$5K, $5K–$10K, $10K–$25K, $25K–$50K, $50K+`.
- Spec `spec/services/revenue_pricing_intelligence_spec.rb`: real aggregation; empty segment; unpriced
  excluded from price stats.
- **Done:** correct aggregates from real deals only; full rspec green; rubocop + brakeman clean.

**WORK + REVIEW + SHIP (committed 0dff010).** Service + spec written; rubocop clean; 7/7 new specs green.
**VERIFIED ON REAL PROD DATA pre-deploy** (ran the actual service over the 48 real closed deals pulled
read-only from the API, as unsaved in-memory Deal instances — no writes): totals won=41 lost=7
win_rate=85% min=$1,000 median=$10,000 max=$109,050 priced_won=41; excluded unpriceable=6;
by_category Sponsorship 40/7/85% · Product 1/0/100%; bands `<$5K 4·100%`, `$5K–$10K 16w/1L/94%`,
`$10K–$25K 13·100%`, `$25K–$50K 6·100%`, `$50K+ 2·100%`. Matches the recon exactly. The 1 priced lost
($5K) correctly lands in $5K–$10K (94% not 100%); the 6 unpriced lost are excluded from bands, counted
in totals/category. ORACLE for post-deploy prod verification. Item 1 logic correct on real deals.
Note: service is inert (referenced by nothing) until items 2+3 wire it — full-suite + brakeman gate runs
once after items 2+3, and the single prod deploy + live-page verification covers all three (a service with
no UI isn't independently verifiable by Brian; route-without-view would error — so they ship as one feature).

---

### ITEMS 2+3 — ROUTE + ACCESS + VIEW (committed 2160915)

**BRAINSTORM/PLAN.** Mirror PipelineController/RenewalsController but with a STRONGER gate: those use
`authorize @organization, :show?` (open to any org member incl. event_guest, data scoped). Pricing must be
full-members-only, so add `before_action :require_pipeline_access` raising `Pundit::NotAuthorizedError`
(→ existing rescue → 302 redirect = "blocked") unless `current_user.can_see_pipeline?(@organization)`.
Deals relation = `policy_scope(@organization.deals)` (DealPolicy::Scope) so restricted-event access is
respected in the aggregate. Nav link inside the existing `unless current_user_is_event_guest?` Revenue
block, beside "Pipeline worklist", active-state keyed on `/pricing_intelligence` (no collision with
`/pipeline`). View = Monaco panel + `overflow-x-auto` tables (totals strip + by-category + by-size-band),
explicit empty-state per segment, caption "real closed deals only · unpriced excluded · not a forecast",
excluded-unpriced footnote.

**WORK.** `PricingIntelligenceController` (auth → set_organization → require_pipeline_access; index =
`authorize :show?` + build pricing). Route `get "pricing_intelligence"` beside pipeline/renewals. Nav link.
View `index.html.erb` + `_segment_table.html.erb` partial (rendered twice). Request spec + system spec.

**REVIEW (all gates green).** rubocop -a then clean (714 files, 0 offenses). brakeman: 0 warnings.
**Full rspec 1711 examples, 0 failures, 1 pending** (pre-existing; 1711 = last night's 1696 + 15 new:
7 service + 6 request + 2 system). Hostile self-review of full diff: additive only, no migration, no API
route/field change, fail-closed gate, no N+1 (in-memory over 48 rows), no invented numbers, nav active-state
non-colliding (verified visually). Request matrix proves: owner 200 with real $20,000 median + $30,000 max
rendered; full member 200; event_guest 302 (blocked); auth required 302; `/api/v1/deals` still 200 with key
(ApiPolicyContext unaffected); restricted Ironclad deal ($77,000) IN for allow-listed Brian, OUT for blocked
Benji. System spec: no horizontal overflow at 1440 + 390, empty `$50K+` band renders "No closed deals";
screenshots saved (desktop + mobile). **Visual check (desktop screenshot):** clean Monaco page, real numbers,
empty-state visible, nav highlights only Pricing intelligence.

**SHIP.** Committed 0dff010 (service) + 2160915 (view/route/access). Clean fast-forward (`git merge-base
--is-ancestor origin/main HEAD` true; origin/main unchanged at 98df743 → no co-tenant collision this run).
Pushed `98df743..2160915 → main`. Render deploy `dep-d8r0uijbc2fs73eac9bg` → **LIVE 00:00 ET**.

**VERIFY IN PRODUCTION (00:00 ET) — all green:**
- `/up` → 200 (health unchanged).
- `GET /organizations/:id/pricing_intelligence` (unauth) → **302 → /users/sign_in** (route deployed + auth
  gate working, NOT a 404).
- `GET /api/v1/deals` (automation key) → **200** (ApiPolicyContext path unaffected).
- **GOLD STANDARD — read-only prod one-off (`job-d8r10c6gvqtc73ecme4g`, succeeded) ran the LIVE deployed
  `Revenue::PricingIntelligence` over the real prod DB:**
  `PIPROOF org="Kerri Media Group" won=41 lost=7 win_rate=85% min=$1,000 median=$10,000 max=$109,050
  priced_won=41 excluded=6; cats sponsorship 40/7/85%/$9,000 · product 1/0/100%/$109,050; bands
  <$5K 4·100% · $5K–$10K 16w/1L/94% · $10K–$25K 13·100% · $25K–$50K 6·100% · $50K+ 2·100%`.
  **Matches the pre-deploy oracle exactly** — the running production app computes the real demand curve correctly.

### COMPOUND (done) + close-out
- **Build learning appended** to `savant-build-learnings.md` (2026-06-19 revenue/pricing-intelligence):
  the stronger-than-:show? full-member gate, feed services `policy_scope(deals)` so restricted-event access
  flows into aggregates, deal demand-curve facts, verify-aggregation-on-real-data-pre-deploy,
  Capybara-vs-microlabel-uppercase, worktree-rebase-onto-origin-main.
- **PATTERN-DOC CANDIDATE (note for the human, promotion is reviewed):** "behind-login screen verification =
  seeded system spec screenshotting at 1440 + 390 asserting `scrollWidth <= innerWidth`" now recurs across
  pipeline-worklist, mobile-shell, design-system, and pricing-intelligence (≥4 Learnings) — ripe to promote
  into the Patterns section as the standing verification recipe for any new behind-login surface.

## RESULT — ALL 3 ITEMS SHIPPED + PROD-VERIFIED. Run #2 complete ~00:05 ET. No regressions (1711/0).
No prod incident; no rollback needed. Items 1 (service), 2 (route+access), 3 (view) all live.

