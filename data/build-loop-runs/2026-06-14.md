# Build-loop run — 2026-06-14 (overnight, into 2026-06-15)

Runner: Kerri (Claude Code, interactive build-loop). Repo: `~/Projects/kerrihq-rails`.
Goal: **proof-of-performance renewal brief** — each renewing sponsor's evidence is one query an agent can draft from.

**Session-start SHA (origin/main, prod-live):** `99298a2`
**Stop rails:** 07:00 ET hard; wrap-up 06:30 ET; no new item after 05:30 ET.
**Hard rails:** additive only (no rename/drop, no destructive migration, never remove an existing /api/v1 field or route); real data only (no invented numbers; phantom-pipeline); UI only inside existing Savant nav; every number traces to a record; rubocop + full rspec + brakeman green before each ship; ship each item independently with prod verification; park anything consequential beyond scope.

Items (fixed order, safest first):
- ITEM 1 — `GET /api/v1/deals/:id/renewal_brief` (read-only, deals:read)
- ITEM 2 — expandable receipts block per row on the existing renewals worklist (Revenue nav)

---

## Timeline

- **2026-06-14 23:21 ET** — START. Read SKILL.md, NOW.md, brain/log.md tail. Confirmed env (ruby 3.4.9, pg up). Verified origin/main = local HEAD = `99298a2`; the renewals API + worklist screen + papered-fix are all already prod-live (the foundation Items 1+2 extend). Created branch `renewal-brief-item1-2026-06-14` off origin/main.

---

## ITEM 1 — GET /api/v1/deals/:id/renewal_brief

### Phase 1 — BRAINSTORM (23:35 ET)

Approaches considered:

1. **Inline serializer in a new controller action** (mirrors `renewals#index`'s `renewal_json`). Lowest ceremony, but the proof_points + owed-breakdown logic is meatier than a flat row and deserves isolated unit tests. Rejected as the home for the logic.
2. **Model method on Deal** (like `fulfillment_summary`). Bloats an already-large model with view-shaped assembly; harder to test in isolation. Rejected.
3. **Service object `Crm::RenewalBrief`** returning the structured hash, with a thin controller action. Matches the existing `Crm::DealHealth` / `Crm::HygieneScorer` pattern exactly, purely additive (new file), directly unit-testable, no blast radius on existing code. **CHOSEN.**

Data audit (all associations confirmed to exist, real-data-only):
- `newsletter_placements` → `delivered?` (delivered_on OR issue published+past), `delivery_status`, `committed?`, `newsletter_issue` (issue_date, display_name).
- `case_file_entries` → kind, occurred_on, summary, source.
- `account_signals` → `engagement?` subset (ENGAGEMENT_SIGNAL_TYPES), signal_type, subject, occurred_at, strength.
- `contract_deliverables` → contracted_count, delivered_count, owed_count, delivery_status.
- `contracts` → `already_signed?`/signed/active → `papered?`.
- `fulfillment_summary` → {contracted, delivered, owed}; `renewal_days_left`, `renewal_status`.

No invented numbers: every receipt string is a count of real rows; a deal with no signal emits an empty `proof_points` array. `owed_before_renewal` is derived from the same committed-placement + contract-deliverable sets `fulfillment_summary` already uses, with each line carrying its record id.

### Phase 2 — PLAN

- NEW `app/services/crm/renewal_brief.rb` — `Crm::RenewalBrief.new(deal).call` → structured hash:
  - identity: deal_id, deal_name, company_id, company_name, value, contract_end_date, renewal_days_left, renewal_status, papered, primary_contact.
  - `fulfillment_summary` (reuse `deal.fulfillment_summary`).
  - `proof_points`: [] of plain-text receipts (delivered newsletter placements, case-file entries, account signals, engagement signals, delivered SOW units, signed contract on record) — only emitted when count > 0, correctly pluralized.
  - `owed_before_renewal`: { total, items: [{type, id, label, …}] } — owed committed placements + owed contract deliverables; total == fulfillment_summary owed.
  - detail arrays: newsletter_placements, case_file_entries, contract_deliverables, contracts, engagement_signals + account_signal_count / engagement_signal_count.
- NEW controller action `Api::V1::DealsController#renewal_brief` (member, deals:read) → `render_one(@deal, …)` style, scoped to org via `set_deal`.
- ROUTE: `member { get :renewal_brief }` on `resources :deals` inside the `/api/v1` namespace block.
- NEW spec `spec/requests/api/v1/deal_renewal_brief_spec.rb` — payload shape on a rich deal (Eight-Sleep-like: delivered + owed placements, case-file entries, engagement signal, signed contract, owed SOW unit), auth gate (deals:read required, missing auth 401), org scoping (404 cross-org), and a sparse deal (empty proof_points, zeroed fulfillment, empty owed items).
- VERIFICATION: `bin/rubocop`, full `bundle exec rspec`, `bin/brakeman --quiet` green; then prod — hit `/api/v1/deals/1c723603.../renewal_brief` (Eight Sleep), Jiga `1f2dc64b...`, and EMI; confirm real proof_points + owed-before-renewal, deals:read-gated.

**DONE (one sentence):** an agent can GET one URL per renewing deal and get back real, record-traceable proof_points + what's owed before renewal, with no existing field or route removed.

### Phases 3–5 — RESULT: ✅ SHIPPED

Files: NEW `app/services/crm/renewal_brief.rb` (Crm::RenewalBrief), NEW action `Api::V1::DealsController#renewal_brief`, route `member get :renewal_brief` on deals, NEW spec `spec/requests/api/v1/deal_renewal_brief_spec.rb` (5 examples).

Self-review fix: guarded `contract.total_value` against a null `total_value_cents` (column default 0 but nullable) so a real contract can never 500 the brief; null stays null (honest, no fabrication).

Gates: `bin/rubocop` 676 files 0 offenses; `bundle exec rspec` **1577 examples, 0 failures, 1 pending** (pending pre-existing; +5 new passing); `bundle exec brakeman --summary` **Security Warnings: 0**. (`bin/brakeman` binstub fails on `--ensure-latest` vs upstream 8.0.5 — pre-existing quirk, not a finding.)

Ship: commit `593c6ea`, fast-forwarded onto `main` (`99298a2..593c6ea`), pushed. Render deploy `dep-d8nn3m0jo6nc73e4ke7g` → **live** 2026-06-14 23:33 ET.

PROD PROOF (`GET /api/v1/deals/:id/renewal_brief`, agent key, deals:read):
- `/up` 200.
- **Eight Sleep** `1c723603` — papered=true, days_left 15, owed=4 (items: "Fireside chat — 1 of 1 owed", "Other — 3 of 3 owed"), proof_points ["1 engagement signal","Signed contract on record"]. Matches the signed SOW (1 fireside + 3 Primary sends, all owed).
- **Jiga** `1f2dc64b` — owed=9 (9 newsletter placements with real Saturday issue dates 6/13–onward), proof_points ["3 case-file entries","1 engagement signal"], papered=false. Matches the "Jiga 9 owed" deliverables ledger.
- **EMI** `1661474e` (Express Manufacturing) — sparse: 1 case-file entry, owed=0, no placements/contracts; proof_points ["1 case-file entry"]. Real data, nothing fabricated.

Branch `renewal-brief-item1-2026-06-14` retained locally (merged, no longer needed).

---

## ITEM 2 — expandable receipts block on the renewals worklist

### Phase 1 — BRAINSTORM (23:45 ET)

The renewals worklist screen already exists (Savant → Revenue → Renewals; shipped 6/14 as `1f95826`) with a summary "Receipts" column (chips: delivered/owed/metric/engaged). The ask: add an *expandable* receipts block per row showing proof_points + owed-before-renewal, every line traceable to a record.

Approaches for the disclosure:
1. **Reuse `accordion_controller.js` (RailsBlocks Stimulus).** Built around an item/trigger/content target tree; fiddly and brittle inside a `<table>`, and a JS failure would hide receipts. Rejected.
2. **Second hidden `<tr>` toggled by JS.** Cross-row `<details>` is invalid HTML; needs custom JS; more moving parts. Rejected.
3. **Native `<details>`/`<summary>` inside the existing Receipts `<td>`.** Zero JS dependency, accessible by default, content wraps vertically in the cell. The table is already wrapped in `overflow-x-auto`, so vertical growth cannot cause horizontal page overflow. **CHOSEN.**

Data source: reuse `Crm::RenewalBrief` (Item 1) so the screen and the API are one source of truth — no second, divergent on-screen calculation. Rows with no receipts omit the block.

### Phase 2 — PLAN

- Controller: preload the brief's associations (+`:primary_contact`, `:case_file_entries`) and precompute `@briefs = {deal.id => Crm::RenewalBrief.new(deal).call}`.
- View: in the Receipts cell, after the summary chips, render a native `<details>` "Proof of performance" → proof_points list (green check) + "Owed before renewal · N" list (warn dot, record-traced labels). Omit when both empty.
- Em-dash fix: switch generated owed-line label separators from em dash to colon (these strings flow through the API into agent-drafted renewal emails; Kerri output must never contain em dashes).
- Specs: request spec asserts block renders proof_points + owed lines + that a receipt-less deal omits it; NEW system spec (headless Chrome) proves no horizontal overflow at desktop 1440 and mobile 390, collapsed and expanded.
- **DONE (one sentence):** on the existing renewals worklist, each sponsor row can expand to show its real receipts and what's owed before renewal, with no new route/surface and no horizontal overflow at 1440 or 390.

### Phases 3–5 — RESULT: ✅ SHIPPED

Files: `app/controllers/renewals_controller.rb` (preload + @briefs), `app/views/renewals/index.html.erb` (native `<details>` block), `app/services/crm/renewal_brief.rb` (colon labels), `spec/requests/renewals_spec.rb` (+2 examples), NEW `spec/system/renewals_worklist_spec.rb` (2 examples, headless Chrome — first system spec in the repo; Chrome + capybara + selenium already bundled).

Gates: `bin/rubocop` 677 files 0 offenses; `bundle exec rspec` **1581 examples, 0 failures, 1 pending** (+4 from Item 2; pending pre-existing); `bundle exec brakeman --summary` **Security Warnings: 0**.

Ship: commit `1eacf1c`, fast-forwarded onto `main` (`593c6ea..1eacf1c`), pushed. Render deploy live 2026-06-14 23:43 ET.

PROD PROOF:
- `/up` 200; `GET /api/v1/renewals` still returns count 5 (no regression).
- `GET /api/v1/deals/1f2dc64b…/renewal_brief` (Jiga) owed labels now colon-format ("Newsletter placement: Hardware FYI Saturday Issue 2026-06-13"); Eight Sleep "Fireside chat: 1 of 1 owed", "Other: 3 of 3 owed"; **zero em dashes confirmed** in labels.
- Worklist screen (login-gated) verified per contract via request spec (block + record-traced lines render) + headless-Chrome system spec (no horizontal overflow at 1440 and 390). Visual render captured to `/tmp/renewals_worklist_receipts.png` and sent to Brian.

Branch `renewal-brief-item2-2026-06-14` retained locally (merged).

---

## RUN SUMMARY — both items SHIPPED, all gates green, prod verified. No rollbacks, no parks. Nothing consequential beyond scope encountered.
