# Build Loop — 2026-06-20 (overnight, Brian /build-loop, unsupervised)

Runner: Kerri (Claude Code). Start 00:18 ET. Default hard stop 07:00 ET (wrap 06:30, no new item after 05:30).
Invocation: "extensive overview of Savant; simplify the busy front-facing dashboard; revenue quick-view in the pipeline; we do media sales; loop brainstorm→plan→work→review→compound."

Isolation: worktree `~/Projects/kerrihq-rails-bl`, branch `bl/overview-revenue` off `origin/main` (tip 2160915, has pricing-intel). Dedicated DB `kerrihq_rails_bl` via DATABASE_URL. Deploy via `git push origin HEAD:main`. Co-tenant check: main checkout clean on `main` a2d1331 at start; no active co-tenant observed.

Context read: savant-build-learnings.md (all), compound-engineering.md, NOW.md, log tail. This is run #3 tonight — runs #1 (multi-user access matrix) + #2 (pricing intelligence) already shipped + prod-verified earlier this evening.

## Item ordering (safest/highest-certainty first)
1. **Item B — Revenue quick-view on the pipeline** (deals kanban). Pure-additive new service + view band, mirrors the just-shipped Revenue::PricingIntelligence pattern. Lowest risk, high certainty.
2. **Item A — Simplify the Overview** (collapse dense event cards behind zero-JS `<details>`). Touches Brian's daily home page; additive/reversible (nothing removed, just collapsed by default).

---

## Item B — Revenue quick-view on the pipeline

### Phase 1 BRAINSTORM
Learnings consulted: pricing-intelligence (PORO `call`, string keys, real-data-only, finance gate via `can_see_pipeline?`), deal-brief (reuse model methods, nil-safety), phantom-data rule (surface excluded/unpriced, never invent).

The main deals kanban (`deals#index`, titled "Pipeline" — the board Brian calls "the pipeline") has a filter bar then the kanban, with NO revenue summary. Brian wants a "revenue quick-view." Real fields available: `Deal#value`, `Deal#probability`, `Deal#weighted_value` (= value*prob/100, returns 0 if either nil), stage enum + OPEN/CLOSED scopes.

Approaches:
- (A) Inline computation in the view from `@deals_by_stage`. Rejected: logic in ERB, not testable, drifts from any future API.
- (B) **CHOSEN: `Revenue::PipelineSnapshot` PORO service** (`.new(deals).call`, string-keyed hash) over the deals already loaded for the board, so the band reflects the active filter (deal_type/event) and the board it sits above. Returns: open_count, open_value, weighted_forecast, unpriced_open_count, won_value, by_stage (open stages → count/value/weighted). Thin assignment in `DealsController#index`; compact finance-gated band partial above the kanban. Mirrors PricingIntelligence exactly (testable, reusable on the worklist later, single source of truth).
- (C) Add a new API endpoint too. Descoped tonight (no agent asked for it); the service is API-ready if wanted later.

Phantom-data discipline: weighted_forecast sums real `weighted_value` only; deals with nil value contribute 0 and are surfaced as `unpriced_open_count` so the open_value isn't misread as complete. No estimated probabilities invented — nil probability → 0 weighted, counted as unpriced/unweighted.

Finance gate: show $ only to users with finance access (same `@show_finance` logic as the dashboard) — the external partner (event_guest) must never see pipeline dollars. Reuse the dashboard's resolver-based gate.

### Phase 2 PLAN
- `app/services/revenue/pipeline_snapshot.rb` — PORO, `initialize(deals)` (any Deal enumerable), `call` → string-keyed hash. Open stages from `Deal::OPEN_STAGES`.
- `spec/services/revenue/pipeline_snapshot_spec.rb` — real-shaped factory deals: priced/unpriced, with/without probability, across stages; assert open_value, weighted_forecast (only priced+prob), unpriced_open_count, by_stage, won_value, empty case.
- `DealsController#index`: compute `@show_finance` (extract the dashboard gate into a shared place or replicate minimally) + `@pipeline_snapshot = Revenue::PipelineSnapshot.new(@deals).call` (only when finance-visible, to avoid wasted work).
- `app/views/deals/_pipeline_snapshot.html.erb` — compact band: BOOKED-context-free (this is pipeline, not booked) → OPEN PIPELINE value + count, WEIGHTED FORECAST, per-stage mini-breakdown, unpriced note. Render above the kanban, inside finance gate.
- DONE = on /organizations/:id/deals a finance user sees a compact revenue band (open value, weighted forecast, by-stage) that matches the deals on the board; an event_guest sees no dollars; numbers trace to records; full suite green; 1440/390 no overflow.

### Phase 3 WORK + Phase 4 REVIEW — DONE
Built Revenue::PipelineSnapshot + _pipeline_snapshot band, wired into deals#index inside the kanban-board turbo frame (refreshes on filter). Gate `can_see_pipeline?` (no-op safety). Specs: 6 service unit + 3 request + 2 system (1440/390 no overflow). rubocop clean, brakeman 0, full suite 1720/0 (1711 baseline + 9). Commit 5c5e7ba.

GOTCHA HIT (worth a Learning): a `<%# %>` comment OR an inline `# prose` comment on the FIRST executable line of a partial blows up this app's render path (ActionView SyntaxErrorInTemplate, prism carets) even though the file compiles fine under standalone erubi — Ruby keywords in the prose (`not`, `in`) get parsed as code. Mid-file `<%# %>` comments are fine (worklist uses them). Fix: start partials with a bare code tag; keep doc in the controller/service. Microlabel uppercase gotcha recurred (assert `/revenue quick-view/i`).

### Phase 5 SHIP — pushed 5c5e7ba → origin/main; Render dep-d8r1hp3sq97s73el96ug build_in_progress. Prod verify pending.

---

## Item A — Simplify the Overview dashboard

### Phase 1 BRAINSTORM
The Overview was already simplified once (6/12 zones 1-3). The remaining density is the per-upcoming-event cards: each `_event_section` = a revenue-goal bar + a 4-tile stat grid (tickets/gross/sponsorship/pipeline) + a 7-button action toolbar (Sponsor Search/Outreach/Pipeline, Ticket Plan/Lead Search/Outreach/Sales). With 2-3 upcoming events that wall repeats 2-3x — the actual "too busy."

Approaches:
- (A) Slim the card: move just the 7-button toolbar behind a disclosure, keep stat grid visible. Smaller win; the 4-tile grid still repeats.
- (B) **CHOSEN: collapse each event into a compact summary ROW** (name · dates · key money + % · countdown) with the full card (revenue bar + stat grid + toolbar) behind a native zero-JS `<details>/<summary>`. Strongest declutter, nothing removed (one click to full detail), reversible, matches the proven worklist `<details>` idiom. "Happening now" events default `open` so a live event stays expanded.
- (C) Remove the event cards from the home page entirely (they live on each event page). Rejected: removes daily at-a-glance signal Brian may rely on; B keeps the key number visible in the summary.

Phantom-data: summary shows only real `total_booked_revenue` / `revenue_goal` / `sponsorship_revenue` already computed; nothing invented.

### Phase 2 PLAN
- Rewrite `app/views/dashboard/_event_section.html.erb`: `<details class="panel group" open?=happening_now>` → `<summary>` compact row (chevron, name, multi-org chip, date range, key money+% or sponsorship $, countdown/Now badge) + body = the EXISTING revenue bar + stat grid + toolbar verbatim (just relocated under the disclosure, original panel-header replaced by the summary).
- Keep the countdown as a static day count in the summary (drops the per-second Stimulus tick = less noise; the live ticker isn't needed for a days-away event).
- System spec: seed an upcoming event; assert the summary row + key number render, the detail (a toolbar link) is in the DOM, expanding shows it, and no horizontal overflow at 1440/390.
- DONE = the Overview shows one calm summary line per upcoming event by default; clicking expands the full stats + actions; nothing removed; no overflow; suite green.

### Phase 3-6 — DONE
Rebuilt `_event_section.html.erb` as a `<details>` disclosure (summary row + body = existing bar/grid/toolbar verbatim). Dropped countdown Stimulus controller on this screen. Updated dashboard_spec countdown example. System spec (collapsed/expanded/overflow) green. Full suite 1724/0, rubocop+brakeman clean. Tidied a stray em dash in PipelineSnapshot comment. Commit e9d7fb7, pushed → Render dep-d8r1ms8jo6nc73cf3fbg building.

## Item B — prod VERIFIED
/up 200 + deployed `Revenue::PipelineSnapshot` run locally over the real `/api/v1/deals` pull (109 deals): open 61 / $223,500, weighted $29,000, 47 unpriced surfaced, 9 unweighted, won 41 / $653,422.41; by-stage proposal_sent 10/$147K, contract_sent 1/$44K (Duro), negotiation 1/$15K (Aris Machina) — matches NOW.md's known figures exactly. Remote Render one-off was blocked by the auto-mode classifier (not worked around); used the local-service-over-real-API-data pattern instead (same as pricing-intel).

## COMPOUND — 2 learnings appended to savant-build-learnings.md (pipeline-quickview; overview-event-disclosure incl. the leading-ERB-comment landmine).

---

## Item A — prod VERIFIED: live (e9d7fb7), /up 200; render path covered by green browser system specs at 1440/390 + dashboard request specs (behind-login per Phase 5).

---

## Item C — revenue band on the Pipeline worklist (mechanical reuse)
Brainstorm/plan: same `Revenue::PipelineSnapshot` + `deals/_pipeline_snapshot` partial, now on `/pipeline` (gap-closing worklist) above the row list, gated `can_see_pipeline?`. Pure reuse on a read-only screen, no JS, lowest risk. Considered per-stage $ subtotals on the kanban columns instead but the drag JS live-updates the column count (`deal-count-<stage>`), so a static $ would desync on drag → would require touching the drag JS (higher risk per the kanban-drag learning) → SKIPPED, noted as a future supervised item. Spec: worklist renders the band with gap-closing open/weighted totals + surfaces the unpriced deal. Full suite 1725/0, rubocop+brakeman clean. Commit 216c177, pushed. No separate learning (mechanical reuse of the Item B pattern).

Skipped/parked future candidates: (1) per-stage $ subtotals on the kanban columns (needs drag-JS work); (2) expected-close / "closing this quarter" forecast view (uses expected_close_date); (3) by-property revenue cut (Hardware FYI / Kinetic / Savant) on the snapshot.

---

## Item D — goal/gap framing on the deals-board band
Added an optional `goal` local to the band partial. On the UNFILTERED deals board only (where the band's `won_value` == org booked), it renders Goal / Booked / Gap-to-goal + "open pipeline covers N% of the gap" + a progress bar. A filtered board passes no goal (won-on-board ≠ booked then), and the worklist passes no goal → both unchanged. Real numbers only. Spec: goal line present unfiltered, absent when filtered; worklist + system specs still green. Full suite 1726/0, rubocop+brakeman clean, em-dash clean. Commit 4448fee, pushed → Render building. COMPOUND: 1 learning appended (the won==booked-only-when-unfiltered constraint).

## Final state: 4 items SHIPPED to prod main (5c5e7ba B, e9d7fb7 A, 216c177 C, 4448fee D), all LIVE on Render. Full suite 1726/0/1pending throughout. Isolated worktree bl/overview-revenue + dedicated DB; every push fast-forward (no co-tenant collision all night).

Morning report: Console task **671e0110-3c78-41d0-8928-b1a8aa042d33** (action_needed, kerri-media-group, read-only — no decision required; offers 3 greenlight-optional next items). An accidental double-create dup (e748942f) was closed (done, with proof note).

## STOP rationale (deliberate, ~01:10 ET): all explicit asks delivered + verified + a bonus; prod pristine/all-green. Remaining candidates are either risky (drag-JS column subtotals) or larger/opinionated or add density that fights the "simplify" goal (close-date forecast, property cut) — per compound-engineering, deferred to Brian via the report's proposed backlog rather than built unattended. Loop can resume on his greenlit picks. NO external sends, no invented data, zero damage.
